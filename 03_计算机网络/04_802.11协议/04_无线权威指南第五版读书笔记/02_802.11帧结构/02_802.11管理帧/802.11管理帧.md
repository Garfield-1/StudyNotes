# 802.11管理帧

[TOC]

## 概述

无线终端使用管理帧加入和离开基本服务集，有线网络则不需要因为通过断开和连接网络电缆就可以实现相同目的。`802.11`管理帧又称为`MMPDU`，它不携带任何上层信息。`MMPDU`帧体并未封装`MSDU`，仅包含二层信息字段和信息元素。信息字段是固定长度的强制字段，而信息元素是可变长度的可选字段

`802.11`帧在帧头的类型和子类型字段共同定义了一个帧

### 类型(Type)字段

| 比特值 | 帧类型 | 作用                       |
| ------ | ------ | -------------------------- |
| `00`   | 管理帧 | 发现接入点并加入基本服务集 |
| `01`   | 控制帧 | 确认传输成功并保留无线介质 |
| `10`   | 数据帧 | 携带上层`MSDU`净荷         |
| `11`   | 扩展帧 | 一种新的、灵活的帧格式     |

### 管理帧子类型(Subtype)字段

| 比特值 | 帧类型                                   | 作用                                                         |
| ------ | ---------------------------------------- | ------------------------------------------------------------ |
| `0000` | **关联请求**(`Association Request`)      | `STA`向`AP`请求加入`BSS`                                     |
| `0001` | **关联响应**(`Association Response`)     | `AP`回应`STA`的关联请求(成功/失败 +`AID`)                    |
| `0010` | **重关联请求**(`Reassociation Request`)  | `STA`在漫游时从一个`AP`切换到另一个`AP`时发送，保留原关联上下文(如`IP`地址) |
| `0011` | **重关联响应**(`Reassociation Response`) | 目标`AP`对重关联请求的回应                                   |
| `0100` | **探测请求**(`Probe Request`)            | `STA`主动扫描时广播，用于发现附近`AP`                        |
| `0101` | **探测响应**(`Probe Response`)           | `AP`收到`Probe Request`后回复自身信息(SSID、速率、能力等)    |
| `0110` | **时间通告帧**(`Timing Advertisement`)   | 用于时间同步(如在`TV`白频段或特定工业应用中)，较少见         |
| `0111` | **保留**(`Reserved`)                     | 未使用                                                       |
| `1000` | **信标帧**(`Beacon`)                     | `AP`定期广播，宣告网络存在，包含`SSID`、`TIM`(省电调度)、能力等 |
| `1001` | **通知传输指示消息帧**(`ATIM`)           | 仅用于`IBSS`(`Ad-hoc`模式)，通知休眠节点有缓存数据           |
| `1010` | **解除关联帧**(`Disassociation`)         | 通知对方终止逻辑连接(但认证仍有效)。可由`STA`或`AP`发起      |
| `1011` | **身份认证帧**(`Authentication`)         | 用于开放系统认证或共享密钥认证(`WEP`时代)，现代`WPA/WPA2`使用`802.1X/EAP`，但此帧仍存在 |
| `1100` | **解除身份认证帧**(`Deauthentication`)   | 终止认证状态(比`Disassociation`更彻底)，常用于踢出客户端或安全断开 |
| `1101` | **动作帧**(`Action`)                     | 用于高级功能交互，如`QoS`(`ADDTS`)、波束成形、无线多跳(`Mesh`)、`TDLS`、`WNM`等 |
| `1110` | **无确认动作帧**(`Action No Ack`)        | 与`Action`类似，但不要求接收方`ACK`(用于广播或多播场景)      |
| `1111` | **保留**(`Reserved`)                     | 未使用                                                       |

## 关联请求帧(Association Request)

客户端向接入点验证自己的身份后，接下来将与接入点建立关联，关联到接入点的客户端成入点发送关联请求帧，请求加入基本服务集；接入点向客户端发送关联响应帧，批准或拒绝客户端的请求

### Association Request帧主体

> 笔者注：Association Request帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.6**

| 顺序   | 信息                                                    | 中文翻译                        | 概述                                                         | IEEE 802.11-2016文档详细解释对应章节或位置 |
| ------ | ------------------------------------------------------- | ------------------------------- | ------------------------------------------------------------ | ------------------------------------------ |
| `1`    | `Capability Information`                                | 能力信息                        | `Capability Information`字段包含若干子字段，用于指示所请求或公布的可选能力 | `9.4.1.4`                                  |
| `2`    | `Listen Interval`                                       | 监听间隔                        | `Listen Interval`字段用于向接入点指示处于节能模式的`STA`在何种频率下唤醒以监听信标帧。该字段的值为`MLME-ASSOCIATE`请求或`MLME-REASSOCIATE`请求原语中的“监听间隔”参数，单位为信标间隔。监听间隔字段的长度为`2`个八位字节 | `9.4.1.5`                                  |
| `3`    | `SSID`                                                  | 服务集标识符                    | `SSID`元素用于标识一个`ESS`(扩展服务集)或`IBSS`(独立基本服务集)的标识信息 | `9.4.2.2`                                  |
| `4`    | `Supported Rates and BSS Membership Selectors`          | 支持的速率与`BSS`成员选择器     | 声明支持的传输速率和加入`BSS`的能力门槛                      | `9.4.2.3`                                  |
| `5`    | `Extended Supported Rates and BSS Membership Selectors` | 扩展支持的速率与`BSS`成员选择器 | 列出额外的速率，还可以包含`BSS`成员资格选择器                | `9.4.2.13`                                 |
| `6`    | `Power Capability`                                      | 功率能力                        | `Power Capability`元素指定了一个`STA`在当前信道中能够进行传输的最小和最大发射功率 | `9.4.2.15`                                 |
| `7`    | `Supported Channels`                                    | 支持的信道                      | `Supported Channels`元素包含一个信道子带列表，表示`STA`能够工作的信道范围 | `9.4.2.18`                                 |
| `8`    | `Robust Security Network`                               | `RSN`                           | 一种仅允许建立健壮安全网络关联(`RSNA`)的安全网络。`RSN`可通过`Beacon frames中 Robust Security Network`元素的指示来识别，即其中指定的组播加密套件不是有线等效保密WEP | `P162`                                     |
| `9`    | `QoS Capability`                                        | `QoS`能力                       | `QoS Capability`元素包含多个子字段，用于在支持`QoS`的站点(`QoS STA`)中通告其可选的`QoS`功能 | `9.4.2.35`                                 |
| `10`   | `RM Enabled Capabilities`                               | `RM`启用的能力                  | `RM Enabled Capabilities`元素用于表明`STA`对无线射频测量的支持 | `9.4.2.45`                                 |
| `11`   | `Mobility Domain`                                       | 移动域                          | `Mobility Domain`元素(`MDE`)包含移动域标识符(`MDID`)字段以及快速切换(`FT`)能力与策略(`FT Capability and Policy`)字段。`AP`使用`MDE`来声明其属于构成某一移动域的`AP`组，通告其对`FT`能力的支持，并发布其`FT`策略信息 | `9.4.2.47`                                 |
| `12`   | `Supported Operating Classes`                           | 支持的操作类别                  | `Supported Operating Classes`元素由`STA`用于通告其在本国所支持的操作类别 | `9.4.2.54`                                 |
| `13`   | `HT Capabilities`                                       | `HT`能力                        | `HT STA`通过发送`HT Capabilities`元素来声明其为`HT`站点      | `9.4.2.56`                                 |
| `14`   | `20/40 BSS Coexistence`                                 | `20/40 BSS`共存                 | `20/40 BSS Coexistence`元素被STA用于交换影响`20/40 MHz BSS`共存的信息。这一元素帮助`STA`之间就共存于同一频段内的操作参数进行信息交流，确保网络性能和稳定性，尤其是在同时支持`20 MHz`和`40 MHz`信道带宽的情况下 | `9.4.2.60`                                 |
| `15`   | `Extended Capabilities`                                 | 扩展能力                        | `Extended Capabilities`元素携带有关`STA`能力的信息，这些信息是对`Capability Information field`中指定能力的补充 | `9.4.2.27`                                 |
| `16`   | `QoS Traffic Capability`                                | `QoS`业务能力                   | `QoS Traffic Capability`元素提供了由非`AP QoS STA`产生的业务类型的信息，并被`QoS AP`用来指示关联的非`AP QoS STA`的访问类别 | `9.4.2.78`                                 |
| `17`   | `TIM Broadcast Request`                                 | `TIM`广播请求                   | `TIM Broadcast Request`元素包含非`AP`站点所请求的周期性`TIM`广播的相关信息 | `9.4.2.83、9.6.14.21`                      |
| `18`   | `Interworking`                                          | 互通功能                        | `Interworking`元素包含了`STA`的互通服务能力的信息            | `9.4.2.92`                                 |
| `19`   | `Multi-band`                                            | 多频段                          | `Multi-band`元素表明发送该元素的`STA`位于一个多频段设备中，该设备能够在不同于当前传输该元素所用频段、运行类别或信道的其他频段、运行类别或信道上工作 | `9.4.2.138`                                |
| `20`   | `DMG Capabilities`                                      | `DMG`能力                       | `DMG Capabilities`元素包含一个STA标识符以及若干字段，用于通告`DMG STA`对可选`DMG`功能的支持情况 | `9.4.2.128`                                |
| `21`   | `Multiple MAC Sublayers`                                | 多个`MAC`子层                   | 本设备在多链路操作中，是否为每个链路使用 独立的`MAC`子层实例 | `9.4.2.153`                                |
| `22`   | `VHT Capabilities`                                      | `VHT`能力                       | `VHT Capabilities`元素声明它是一个`VHT STA`。`VHT`能力元素包含了若干字段，用于通告`VHT STA`的`VHT`能力 | `9.4.2.158`                                |
| `23`   | `Operating Mode Notification`                           | 操作模式通知                    | `Operating Mode Notification元素`用于通知其他`STA`：发送该元素的`STA`正在更改其一个或多个操作参数，包括操作信道带宽、可接收的最大空间流数量以及`LDPC`接收偏好 | `9.4.2.166、9.6.23.4`                      |
| `Last` | `Vendor Specific`                                       | 厂商特定信息                    | `Vendor Specific`元素用于在单一定义的格式内承载本标准中未定义的信息，以避免为非标准用途占用保留的元素标识符，并使得在存在非标准信息的情况下更容易实现互操作性 | `9.4.2.26`                                 |

##  关联响应帧(Association Response)

关联响应帧是`AP`对`STA`关联请求的正式答复，用于批准/拒绝连接、分配`AID`，并同步网络能力参数

### Association Response帧主体

> 笔者注：Association Response帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.7**

| 顺序   | 信息                                                    | 中文翻译                        | 概述                                                         | IEEE 802.11-2016文档详细解释对应章节或位置 |
| ------ | ------------------------------------------------------- | ------------------------------- | ------------------------------------------------------------ | ------------------------------------------ |
| `1`    | `Capability Information`                                | 能力信息                        | `Capability Information`字段包含若干子字段，用于指示所请求或公布的可选能力 | `9.4.1.4`                                  |
| `2`    | `Status code`                                           | 状态码                          | 状态码字段用于响应管理帧中，以指示所请求操作的成功或失败     | `9.4.1.9`                                  |
| `3`    | `AID`                                                   | `AID`(关联标识符)               | `AID`元素包含由接入点(`AP`)在关联过程中分配给`STA`的`AID`，用于表示该`STA`的`16`位标识符 | `9.4.2.164`                                |
| `4`    | `Supported Rates and BSS Membership Selectors`          | 支持的速率与`BSS`成员选择器     | 声明支持的传输速率和加入`BSS`的能力门槛                      | `9.4.2.3`                                  |
| `5`    | `Extended Supported Rates and BSS Membership Selectors` | 扩展支持的速率与`BSS`成员选择器 | 列出额外的速率，还可以包含`BSS`成员资格选择器(`BSS Membership Selectors`) | `9.4.2.13`                                 |
| `6`    | `EDCA Parameter Set`                                    | `EDCA`参数集                    | `EDCA Parameter Set`元素为`STA`在竞争周期(`CP`)内正确运行`QoS功能提供了所需的信息` | `9.4.2.29`                                 |
| `7`    | `RCPI`                                                  | `RCPI`(接收信道功率指示)        | `RCPI`元素表示接收`STA`接收到的帧的功率电平                  | `9.4.2.38`                                 |
| `8`    | `RSNI`                                                  | `RSNI`(接收信号噪声指示)        | `RSNI`元素包含一个`RSNI`(信噪比)值                           | `9.4.2.41`                                 |
| `9`    | `RM Enabled Capabilities`                               | `RM`启用的能力                  | `RM Enabled Capabilities`元素用于表明`STA`对无线射频测量的支持 | `9.4.2.45`                                 |
| `10`   | `Mobility Domain`                                       | 移动域                          | `Mobility Domain`元素(`MDE`)包含移动域标识符(`MDID`)字段以及快速切换(`FT`)能力与策略(FT Capability and Policy)字段。`AP`使用`MDE`来声明其属于构成某一移动域的`AP`组，通告其对`FT`能力的支持，并发布其`FT`策略信息 | `9.4.2.47`                                 |
| `11`   | `Fast BSS Transition`                                   | 快速`BSS`切换                   | 快速切换元素(`FTE`)包含在强健安全网络(`RSN`)中执行快速`BSS`切换期间所需的FT认证序列信息 | `9.4.2.48`                                 |
| `12`   | `DSE registered location`                               | `DSE`注册位置                   | `DSE`注册位置元素包含`DSE`位置配置信息(`LCI`)，其中包括纬度、经度和海拔信息 | `9.4.2.52`                                 |
| `13`   | `Timeout Interval(Association Comeback time)`           | 超时间隔(关联恢复时间)          | `Timeout Interval`元素用于指定时间间隔和超时值               | `9.4.2.49`                                 |
| `14`   | `HT Capabilities`                                       | `HT`能力                        | `HT STA`通过发送`HT Capabilities`元素来声明其为`HT`站点      | `9.4.2.56`                                 |
| `15`   | `HT Operation`                                          | `HT`操作                        | `BSS`中`HT`站点的操作由`HT`操作元素控制                      | `9.4.2.57`                                 |
| `16`   | `20/40 BSS Coexistence`                                 | `20/40 BSS`共存                 | `20/40 BSS Coexistence`元素被`STA`用于交换影响`20/40 MHz BSS`共存的信息。这一元素帮助`STA`之间就共存于同一频段内的操作参数进行信息交流，确保网络性能和稳定性，尤其是在同时支持`20 MHz`和`40 MHz`信道带宽的情况下 | `9.4.2.60`                                 |
| `17`   | `Overlapping BSS Scan Parameters`                       | 重叠`BSS`扫描参数               | 重叠`BSS`扫描参数元素由`AP`使用，用于指示`BSS`成员在执行`OBSS`扫描操作时应使用的值 | `9.4.2.59`                                 |
| `18`   | `Extended Capabilities`                                 | 扩展能力                        | `Extended Capabilities`元素携带有关`STA`能力的信息，这些信息是对`Capability Information field`中指定能力的补充 | `9.4.2.27`                                 |
| `19`   | `BSS Max Idle Period`                                   | `BSS`最大空闲周期               | `BSS`最大空闲周期元素包含了一个时间间隔，表示非`AP STA`在未向`AP`发送任何帧的情况下可保持的最大空闲时间 | `9.4.2.79`                                 |
| `20`   | `TIM Broadcast Response`                                | `TIM`广播响应                   | `TIM`广播响应元素包含有关`AP`周期性`TIM`广播的信息           | `9.4.2.84`                                 |
| `21`   | `QoS Map`                                               | `QoS`映射                       | 用于提供高层服务质量(`QoS`)构造到本标准中定义的数据帧传输所使用的用户优先级(`User Priority`)之间的映射 | `9.4.2.95`                                 |
| `22`   | `QMF Policy`                                            | `QMF`策略                       | `QMF`策略帧采用动作帧(`Action frame`)格式，由请求`STA`发送给接收站点，并包含所附带的QMF策略 | `9.6.8.18`                                 |
| `23`   | `Multi-band`                                            | 多频段                          | `Multi-band元素`表明发送该元素的`STA`位于一个多频段设备中，该设备能够在不同于当前传输该元素所用频段、运行类别或信道的其他频段、运行类别或信道上工作 | `9.4.2.138`                                |
| `24`   | `DMG Capabilities`                                      | `DMG`能力                       | `DMG Capabilities元素`包含一个`STA`标识符以及若干字段，用于通告`DMG STA`对可选`DMG`功能的支持情况 | `9.4.2.128`                                |
| `25`   | `DMG Operation`                                         | `DMG`操作                       | 由`AP`或`PCP`提供的`BSS`运行参数                             | `9.4.2.129`                                |
| `26`   | `Multiple MAC Sublayers`                                | 多个`MAC`子层                   | 本设备在多链路操作中，是否为每个链路使用 独立的`MAC`子层实例 | `9.4.2.153`                                |
| `27`   | `Neighbor Report`                                       | 邻近报告                        | 帮助`STA`发现周围的`AP`信息，从而支持更智能的漫游和网络切换决策 | `9.4.2.37`                                 |
| `28`   | `VHT Capabilities`                                      | `VHT`能力                       | `VHT Capabilities`元素声明它是一个`VHT STA`。`VHT`能力元素包含了若干字段，用于通告`VHT STA`的`VHT`能力 | `9.4.2.158`                                |
| `29`   | `VHT Operation`                                         | 运行模式通知                    | `BSS`中`VHT`站点的操作由`HT`操作元素和`VHT`操作元素共同控制  | `9.4.2.159`                                |
| `30`   | `Operating Mode Notification`                           | 操作模式通知                    | `Operating Mode Notification`元素用于通知其他`STA`：发送该元素的STA正在更改其一个或多个操作参数，包括操作信道带宽、可接收的最大空间流数量以及`LDPC`接收偏好 | `9.4.2.166、9.6.23.4`                      |
| `31`   | `Future Channel Guidance`                               | 未来信道指引                    | 未来信道指引元素由`AP`、`IBSS`站点、网格站点或`PCP`使用，用于在发送`STA`更改操作信道的情况下，指导其他`STA`关于可能的未来信道的信息 | `9.4.2.175`                                |
| `Last` | `Vendor Specific`                                       | 厂商特定信息                    | `Vendor Specific`元素用于在单一定义的格式内承载本标准中未定义的信息，以避免为非标准用途占用保留的元素标识符，并使得在存在非标准信息的情况下更容易实现互操作性 | `9.4.2.26`                                 |

## 重关联请求帧(Reassociation Request)

客户端决定漫游到新接入点时，将向新接入点发送重关联请求帧。客户端利用重关联请求帧从目前的基本服务集切换到新的基本服务集，这种管理帧实际上属于客户端发送给目标接入点的漫游请求

### Reassociation Request帧主体

> 笔者注：Reassociation Request帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.8**

| 顺序   | 信息                                                    | 中文翻译                        | 概述                                                         | IEEE 802.11-2016文档详细解释对应章节或位置 |
| ------ | ------------------------------------------------------- | ------------------------------- | ------------------------------------------------------------ | ------------------------------------------ |
| `1`    | `Capability Information`                                | 能力信息                        | `Capability Information`字段包含若干子字段，用于指示所请求或公布的可选能力 | `9.4.1.4`                                  |
| `2`    | `Listen Interval`                                       | 监听间隔                        | `Listen Interval`字段用于向接入点指示处于节能模式的`STA`在何种频率下唤醒以监听信标帧。该字段的值为`MLME-ASSOCIATE`请求或`MLME-REASSOCIATE`请求原语中的“监听间隔”参数，单位为信标间隔。监听间隔字段的长度为`2`个八位字节 | `9.4.1.5`                                  |
| `3`    | `Current AP address`                                    | 当前`AP`地址                    | 当前`AP`地址字段是该`STA`当前所关联的`AP`的`MAC`地址         | `9.4.1.5`                                  |
| `4`    | `SSID`                                                  | 服务集标识符                    | `SSID`元素用于标识一个`ESS`(扩展服务集)或`IBSS`(独立基本服务集)的标识信息 | `9.4.2.2`                                  |
| `5`    | `Supported Rates and BSS Membership Selectors`          | 支持的速率与`BSS`成员选择器     | 声明支持的传输速率和加入`BSS`的能力门槛                      | `9.4.2.3`                                  |
| `6`    | `Extended Supported Rates and BSS Membership Selectors` | 扩展支持的速率与`BSS`成员选择器 | 列出额外的速率，还可以包含`BSS`成员资格选择器                | `9.4.2.13`                                 |
| `7`    | `Power Capability`                                      | 功率能力                        | `Power Capability`元素指定了一个`STA`在当前信道中能够进行传输的最小和最大发射功率 | `9.4.2.15`                                 |
| `8`    | `Supported Channels`                                    | 支持的信道                      | `Supported Channels`元素包含一个信道子带列表，表示`STA`能够工作的信道范围 | `9.4.2.18`                                 |
| `9`    | `RSN`                                                   | 强健安全网络                    | 一种安全网络，仅允许建立强健安全网络关联(`RSNA`)             | `P162`                                     |
| `10`   | `QoS Capability`                                        | `QoS`能力                       | `QoS Capability`元素包含多个子字段，用于在支持`QoS`的站点(`QoS STA`)中通告其可选的`QoS`功能 | `9.4.2.35`                                 |
| `11`   | `RM Enabled Capabilities`                               | `RM`启用的能力                  | `RM Enabled Capabilities`元素用于表明`STA`对无线射频测量的支持 | `9.4.2.45`                                 |
| `12`   | `Mobility Domain`                                       | 移动域                          | `Mobility Domain`元素(`MDE`)包含移动域标识符(`MDID`)字段以及快速切换(`FT`)能力与策略(`FT Capability and Policy`)字段。`AP`使用`MDE`来声明其属于构成某一移动域的`AP`组，通告其对`FT`能力的支持，并发布其`FT`策略信息 | `9.4.2.47`                                 |
| `13`   | `Fast BSS Transition`                                   | 移动域                          | 快速切换元素(`FTE`)包含在强健安全网络(`RSN`)中执行快速`BSS`切换期间所需的`FT`认证序列信息 | `9.4.2.47`                                 |
| `14`   | `Resource information container(RIC)`                   | 资源信息容器                    | 资源信息容器(`RIC`)指的是一组用于表达资源请求或响应的元素集合 | `13.11.2`                                  |
| `15`   | `Supported Operating Classes`                           | 支持的操作类别                  | `Supported Operating Classes`元素由`STA`用于通告其在本国所支持的操作类别 | `9.4.2.54`                                 |
| `16`   | `HT Capabilities`                                       | `HT`能力                        | `HT STA`通过发送`HT Capabilities`元素来声明其为`HT`站点      | `9.4.2.56`                                 |
| `17`   | `20/40 BSS Coexistence`                                 | `20/40 BSS`共存                 | `20/40 BSS Coexistence`元素被`STA`用于交换影响`20/40 MHz BSS`共存的信息。这一元素帮助`STA`之间就共存于同一频段内的操作参数进行信息交流，确保网络性能和稳定性，尤其是在同时支持`20 MHz`和`40 MHz`信道带宽的情况下 | `9.4.2.60`                                 |
| `18`   | `Extended Capabilities`                                 | 扩展能力                        | `Extended Capabilities`元素携带有关STA能力的信息，这些信息是对`Capability Information field`中指定能力的补充 | `9.4.2.27`                                 |
| `19`   | `QoS Traffic Capability`                                | `QoS`业务能力                   | `QoS Traffic Capability`元素提供了由非`AP QoS STA`产生的业务类型的信息，并被`QoS AP`用来指示关联的非`AP QoS STA`的访问类别 | `9.4.2.78`                                 |
| `20`   | `TIM Broadcast Request`                                 | `TIM`广播请求                   | `TIM Broadcast Request`元素包含非`AP`站点所请求的周期性`TIM`广播的相关信息 | `9.4.2.83、9.6.14.21`                      |
| `21`   | `FMS Request`                                           | `FMS`请求                       | `FMS`请求元素定义了非`AP STA`所请求的组播帧的相关信息        | `9.4.2.76`                                 |
| `22`   | `DMS Request`                                           | `DMS`请求                       | `DMS`请求元素定义了将作为单播帧传输的组播帧的相关信息        | `9.4.2.88`                                 |
| `23`   | `Interworking`                                          | 互通功能                        | `Interworking`元素包含了`STA`的互通服务能力的信息            | `9.4.2.92`                                 |
| `24`   | `Multi-band`                                            | 多频段                          | `Multi-band`元素表明发送该元素的`STA`位于一个多频段设备中，该设备能够在不同于当前传输该元素所用频段、运行类别或信道的其他频段、运行类别或信道上工作 | `9.4.2.138`                                |
| `25`   | `DMGCapabilities`                                       | `DMG`能力                       | `DMG Capabilities`元素包含一个`STA`标识符以及若干字段，用于通告`DMG STA`对可选`DMG`功能的支持情况 | `9.4.2.128`                                |
| `26`   | `Multiple MAC Sublayers`                                | 多个`MAC`子层                   | 本设备在多链路操作中，是否为每个链路使用 独立的`MAC`子层实例 | `9.4.2.153`                                |
| `27`   | `VHT Capabilities`                                      | `VHT`能力                       | `VHT Capabilities`元素声明它是一个`VHT STA`。`VHT`能力元素包含了若干字段，用于通告`VHT STA`的`VHT`能力 | `9.4.2.158`                                |
| `28`   | `Operating Mode Notification`                           | 运行模式通知                    | 操作模式通知元素用于通知`STA`，发送`STA`正在更改其一个或多个操作信道宽度、它可以接收的最大空间流数量以及其`LDPC`接收偏好 | `9.4.2.166`                                |
| `Last` | `Vendor Specific`                                       | 厂商特定信息                    | `Vendor Specific`元素用于在单一定义的格式内承载本标准中未定义的信息，以避免为非标准用途占用保留的元素标识符，并使得在存在非标准信息的情况下更容易实现互操作性 | `9.4.2.26`                                 |

## 重关联响应帧(Reassociation Response)

重关联响应帧是新`AP`对`STA`漫游请求的正式答复，用于批准/拒绝切换、分配新`AID`、同步网络参数，并最终确立`STA`与新`AP`的数据通路

### Reassociation Response帧主体

> 笔者注：Reassociation Response帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.9**

| 顺序   | 信息                                                    | 中文翻译                        | 概述                                                         | IEEE 802.11-2016文档详细解释对应章节或位置 |
| ------ | ------------------------------------------------------- | ------------------------------- | ------------------------------------------------------------ | ------------------------------------------ |
| `1`    | `Capability Information`                                | 能力信息                        | `Capability Information`字段包含若干子字段，用于指示所请求或公布的可选能力 | `9.4.1.4`                                  |
| `2`    | `Status code`                                           | 状态码                          | 状态码字段用于响应管理帧中，以指示所请求操作的成功或失败     | `9.4.1.9`                                  |
| `3`    | `AID`                                                   | `AID`                           | `AID`元素包含由`AP`在关联过程中分配给`STA`的`AID`，用于表示该`STA`的`16`位标识符 | `9.4.2.164`                                |
| `4`    | `Supported Rates and BSS Membership Selectors`          | 支持的速率与`BSS`成员选择器     | 声明支持的传输速率和加入`BSS`的能力门槛                      | `9.4.2.3`                                  |
| `5`    | `Extended Supported Rates and BSS Membership Selectors` | 扩展支持的速率与`BSS`成员选择器 | 列出额外的速率，还可以包含`BSS`成员资格选择器(`BSS Membership Selectors`) | `9.4.2.13`                                 |
| `6`    | `EDCA Parameter Set`                                    | `EDCA`参数集                    | `EDCA Parameter Set`元素为STA在竞争周期(`CP`)内正确运行QoS功能提供了所需的信息 | `9.4.2.29`                                 |
| `7`    | `RCPI`                                                  | `RCPI`(接收信道功率指示)        | `RCPI`元素表示接收`STA`接收到的帧的功率电平                  | `9.4.2.38`                                 |
| `8`    | `RSNI`                                                  | `RSNI`(接收信号噪声指示)        | `RSNI`元素包含一个`RSNI`(信噪比)值                           | `9.4.2.41`                                 |
| `9`    | `RM Enabled Capabilities`                               | `RM`启用的能力                  | `RM Enabled Capabilities`元素用于表明`STA`对无线射频测量的支持 | `9.4.2.45`                                 |
| `10`   | `RSN`                                                   | 强健安全网络                    | 一种安全网络，仅允许建立强健安全网络关联(`RSNA`)             | `P162`                                     |
| `11`   | `Mobility Domain`                                       | 移动域                          | `Mobility Domain`元素(`MDE`)包含移动域标识符(`MDID`)字段以及快速切换(`FT`)能力与策略(`FT Capability and Policy`)字段。`AP`使用`MDE`来声明其属于构成某一移动域的`AP`组，通告其对`FT`能力的支持，并发布其`FT`策略信息 | `9.4.2.47`                                 |
| `12`   | `Fast BSS Transition`                                   | 快速`BSS`切换                   | 快速切换元素(`FTE`)包含在强健安全网络(`RSN`)中执行快速BSS切换期间所需的`FT`认证序列信息 | `9.4.2.48`                                 |
| `13`   | `RIC`                                                   | 资源信息容器                    | 一个`RIC`是一个或多个资源请求(`Resource Request`)的序列，或者是一个或多个资源响应(`Resource Response`)的序列 | `9.4.2.50`                                 |
| `14`   | `DSE registered location`                               | `DSE`注册位置                   | `DSE`注册位置元素包含DSE位置配置信息(`LCI`)，其中包括纬度、经度和海拔信息 | `9.4.2.52`                                 |
| `15`   | `Timeout Interval(Association Comeback time)`           | 超时间隔(关联恢复时间)          | `Timeout Interval`元素用于指定时间间隔和超时值               | `9.4.2.49`                                 |
| `16`   | `HT Capabilities`                                       | `HT`能力                        | `HT STA`通过发送`HT Capabilities`元素来声明其为`HT`站点      | `9.4.2.56`                                 |
| `17`   | `HT Operation`                                          | `HT`操作                        | `BSS`中`HT`站点的操作由`HT`操作元素控制                      | `9.4.2.57`                                 |
| `18`   | `20/40 BSS Coexistence`                                 | `20/40 BSS`共存                 | `20/40 BSS Coexistence`元素被`STA`用于交换影响`20/40 MHz BSS`共存的信息。这一元素帮助`STA`之间就共存于同一频段内的操作参数进行信息交流，确保网络性能和稳定性，尤其是在同时支持`20 MHz`和`40 MHz`信道带宽的情况下 | `9.4.2.60`                                 |
| `19`   | `Overlapping BSS Scan Parameters`                       | 重叠`BSS`扫描参数               | 重叠`BSS`扫描参数元素由`AP`使用，用于指示`BSS`成员在执行`OBSS`扫描操作时应使用的值 | `9.4.2.59`                                 |
| `20`   | `Extended Capabilities`                                 | 扩展能力                        | `Extended Capabilities`元素携带有关`STA`能力的信息，这些信息是对`Capability Information field`中指定能力的补充 | `9.4.2.27`                                 |
| `21`   | `BSS Max Idle Period`                                   | `BSS`最大空闲周期               | `BSS`最大空闲周期元素包含了一个时间间隔，表示非`AP STA`在未向`AP`发送任何帧的情况下可保持的最大空闲时间 | `9.4.2.79`                                 |
| `22`   | `TIM Broadcast Response`                                | `TIM`广播响应                   | `TIM`广播响应元素包含有关`AP`周期性`TIM`广播的信息           | `9.4.2.84`                                 |
| `23`   | `FMS Response`                                          | `FMS`响应                       | `FMS`响应元素提供有关组播帧投递的信息                        | `9.4.2.77`                                 |
| `24`   | `DMS Response`                                          | `DMS`响应                       | `DMS`响应元素提供有关所请求的组播帧的状态信息                | `9.4.2.89`                                 |
| `25`   | `QoS Map`                                               | `QoS`映射                       | 用于提供高层服务质量(`QoS`)构造到本标准中定义的数据帧传输所使用的用户优先级(`User Priority`)之间的映射 | `9.4.2.95`                                 |
| `26`   | `QMF Policy`                                            | `QMF`策略                       | `QMF`策略帧采用动作帧(`Action frame`)格式，由请求`STA`发送给接收站点，并包含所附带的`QMF`策略 | `9.6.8.18`                                 |
| `27`   | `Multi-band`                                            | 多频段                          | `Multi-band`元素表明发送该元素的`STA`位于一个多频段设备中，该设备能够在不同于当前传输该元素所用频段、运行类别或信道的其他频段、运行类别或信道上工作 | `9.4.2.138`                                |
| `28`   | `DMGCapabilities`                                       | `DMG`能力                       | `DMG Capabilities`元素包含一个`STA`标识符以及若干字段，用于通告`DMG STA`对可选`DMG`功能的支持情况 | `9.4.2.128`                                |
| `29`   | `DMG Operation`                                         | `DMG`操作                       | 由`AP`或`PCP`提供的`BSS`运行参数                             | `9.4.2.129`                                |
| `30`   | `Multiple MAC Sublayers`                                | 多个`MAC`子层                   | 本设备在多链路操作中，是否为每个链路使用 独立的`MAC`子层实例 | `9.4.2.153`                                |
| `31`   | `Neighbor Report`                                       | 邻近报告                        | 帮助`STA`发现周围的`AP`信息，从而支持更智能的漫游和网络切换决策 | `9.4.2.37`                                 |
| `31`   | `VHT Capabilities`                                      | `VHT`能力                       | `VHT Capabilities`元素声明它是一个`VHT STA`。`VHT`能力元素包含了若干字段，用于通告`VHT STA`的`VHT`能力 | `9.4.2.158`                                |
| `32`   | `VHT Operation`                                         | 运行模式通知                    | `BSS`中`VHT`站点的操作由`HT`操作元素和`VHT`操作元素共同控制  | `9.4.2.159`                                |
| `33`   | `Operating Mode Notification`                           | 操作模式通知                    | `Operating Mode Notification`元素用于通知其他`STA`：发送该元素的`STA`正在更改其一个或多个操作参数，包括操作信道带宽、可接收的最大空间流数量以及`LDPC`接收偏好 | `9.4.2.166、9.6.23.4`                      |
| `34`   | `Future Channel Guidance`                               | 未来信道指引                    | 未来信道指引元素由`AP`、`IBSS`站点、网格站点或`PCP`使用，用于在发送`STA`更改操作信道的情况下，指导其他`STA`关于可能的未来信道的信息 | `9.4.2.175`                                |
| `Last` | `Vendor Specific`                                       | 厂商特定信息                    | `Vendor Specific`元素用于在单一定义的格式内承载本标准中未定义的信息，以避免为非标准用途占用保留的元素标识符，并使得在存在非标准信息的情况下更容易实现互操作性 | `9.4.2.26`                                 |

## 探测请求帧(Probe Request)

`Probe Request`是一种由`STA`主动发送的管理帧，用于发现附近可用的`AP`

### Probe Request帧主体

> 笔者注：Probe Request帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.10**

| 顺序   | 信息                                                    | 中文翻译                    | 概述                                                         | IEEE 802.11-2016文档详细解释对应章节或位置 |
| ------ | ------------------------------------------------------- | --------------------------- | ------------------------------------------------------------ | ------------------------------------------ |
| `1`    | `SSID`                                                  | 服务集标识符                | `SSID`元素用于标识一个`ESS`(扩展服务集)或`IBSS`(独立基本服务集)的标识信息 | `9.4.2.2`                                  |
| `2`    | `Supported Rates and BSS Membership Selectors`          | 支持的速率与`BSS`成员选择器 | 声明支持的传输速率和加入`BSS`的能力门槛                      | `9.4.2.3`                                  |
| `3`    | `Request`                                               | 请求                        | 此元素放置在`Probe Request frame`或`Information Request frame`中，以请求响应的`STA`分别在`Probe Response frame`或`Information Response frame`中包含所请求的信息 | `9.4.2.10`                                 |
| `4`    | `Extended Supported Rates and BSS Membership Selectors` | `DSSS`参数集                | `DSSS`参数集元素包含用于使`STA`能够识别信道号的信息          | `9.4.2.4`                                  |
| `5`    | `DSSS Parameter Set`                                    | `DSSS`参数集                | `DSSS`参数集元素包含用于使`STA`能够识别信道号的信息          | `9.4.2.4`                                  |
| `6`    | `Supported Operating Classes`                           | 信道使用情况                | 信道使用情况元素定义了非基础设施型`BSS`或非`TDLS`异频直连链路的`BSS`的信道使用信息 | `9.4.2.86`                                 |
| `7`    | `HT Capabilities`                                       | `Mesh ID`                   | `Mesh ID`元素用于通告`MBSS`(`Mesh`基本服务集)的标识          | `9.4.2.99`                                 |
| `8`    | `20/40 BSS Coexistence`                                 | 预估服务参数                | 预估服务参数元素由一个`STA`用来向另一个`STA`提供信息，后者可将该信息作为算法的输入，以估算两个`STA`之间的吞吐量 | `9.4.2.174`                                |
| `9`    | `Extended Capabilities`                                 | 扩展请求                    | 该元素被置于`Probe Request frame`或`Information Request frame`中，以请求响应的`STA`分别在`Probe Response frame`或`Information Response frame`中包含所请求的信息 | `9.4.2.11`                                 |
| `10`   | `SSID List`                                             | `SSID`列表                  | `SSID`列表字段是`STA`请求信息的`SSID`元素的列表              | `9.4.2.73`                                 |
| `11`   | `Channel Usage`                                         | 信道使用情况                | 信道使用情况元素定义了非基础设施型`BSS`或非`TDLS`异频直连链路的`BSS`的信道使用信息 | `9.4.2.86`                                 |
| `12`   | `Interworking`                                          | 互通功能                    | `Interworking`元素包含了`STA`的互通服务能力的信息            | `9.4.2.92`                                 |
| `13`   | `Mesh ID`                                               | `Mesh ID`                   | `Mesh ID`元素用于通告`MBSS`(`Mesh`基本服务集)的标识          | `9.4.2.99`                                 |
| `14`   | `Multi-band`                                            | 多频段                      | `Multi-band`元素表明发送该元素的`STA`位于一个多频段设备中，该设备能够在不同于当前传输该元素所用频段、运行类别或信道的其他频段、运行类别或信道上工作 | `9.4.2.138`                                |
| `15`   | `DMG Capabilities`                                      | `DMG`能力                   | `DMG Capabilities`元素包含一个`STA`标识符以及若干字段，用于通告`DMG STA`对可选`DMG`功能的支持情况 | `9.4.2.128`                                |
| `16`   | `Multiple MAC Sublayers`                                | 多个`MAC`子层               | 本设备在多链路操作中，是否为每个链路使用 独立的`MAC`子层实例 | `9.4.2.153`                                |
| `17`   | `VHT Capabilities`                                      | `VHT`能力                   | `VHT Capabilities`元素声明它是一个`VHT STA`。`VHT`能力元素包含了若干字段，用于通告`VHT STA`的`VHT`能力 | 9.4.2.158                                  |
| `18`   | `Estimated Service Parameters`                          | 预估服务参数                | 预估服务参数元素由一个`STA`用来向另一个`STA`提供信息，后者可将该信息作为算法的输入，以估算两个`STA`之间的吞吐量 | `9.4.2.174`                                |
| `19`   | `Extended Request`                                      | 扩展请求                    | 该元素被置于`Probe Request frame`或`Information Request frame`中，以请求响应的`STA`分别在`Probe Response frame`或`Information Response frame`中包含所请求的信息 | `9.4.2.11`                                 |
| `Last` | `Vendor Specific`                                       | 厂商特定信息                | `Vendor Specific`元素用于在单一定义的格式内承载本标准中未定义的信息，以避免为非标准用途占用保留的元素标识符，并使得在存在非标准信息的情况下更容易实现互操作性 | `9.4.2.26`                                 |

## 探测响应帧(Probe Response)

`Probe Response`是一种由`AP`发送的 管理帧，用于响应`STA`发送的`Probe Request`

### Probe Response帧主体

> 笔者注：Probe Response帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.11**

| 顺序     | 信息                                                    | 中文翻译                        | 概述                                                         | IEEE 802.11-2016文档详细解释对应章节或位置 |
| -------- | ------------------------------------------------------- | ------------------------------- | ------------------------------------------------------------ | ------------------------------------------ |
| `1`      | `Timestamp`                                             | 时间戳                          | 帧源端的定时同步功能(`TSF`)计时器的值                        | `9.4.1.10`                                 |
| `2`      | `Beacon interval`                                       | 信标间隔                        | 目标信标传输时间(`TBTT`)之间的时间单位(`TU`)数量             | `9.4.1.3`                                  |
| `3`      | `Capability Information`                                | 能力信息                        | `Capability Information`字段包含若干子字段，用于指示所请求或公布的可选能力 | `9.4.1.4`                                  |
| `4`      | `SSID`                                                  | 服务集标识符                    | `SSID`元素用于标识一个`ESS`(扩展服务集)或`IBSS`(独立基本服务集)的标识信息 | `9.4.2.2`                                  |
| `5`      | `Supported Rates and BSS Membership Selectors`          | 支持的速率与BSS成员选择器       | 声明支持的传输速率和加入`BSS`的能力门槛                      | `9.4.2.3`                                  |
| `6`      | `DSSS Parameter Set`                                    | `DSSS`参数集                    | `DSSS`参数集元素包含用于使`STA`能够识别信道号的信息          | `9.4.2.4`                                  |
| `7`      | `CF Parameter Set`                                      | `CF`参数集                      | `CF`参数集元素包含支持`PCF`所需的一组参数                    | `9.4.2.5`                                  |
| `8`      | `IBSS Parameter Set`                                    | `IBSS`参数集                    | `IBSS`参数集元素包含支持`IBSS`所需的一组参数                 | `9.4.2.7`                                  |
| `9`      | `Country`                                               | 国家信息                        | `Country`元素包含允许`STA`识别其所在监管域并配置其物理层以在该监管域内操作所需的信息 | `9.4.2.9`                                  |
| `10`     | `Power Constraint`                                      | 功率限制                        | 功率限制元素包含必要的信息，用于使`STA`能够确定当前信道中的本地最大发射功率 | `9.4.2.14`                                 |
| `11`     | `Channel Switch Announcement`                           | 信道切换公告                    | `Channel Switch Announcement`元素用于通告其即将切换到的新信道及新信道的信道号 | `9.4.2.19`                                 |
| `12`     | `Quiet`                                                 | 静默                            | `Quiet`元素定义了当前信道中不进行任何传输的时间间隔。该时间间隔可用于辅助进行信道测量，避免受到同一`BSS`中其他`STA`的干扰 | `9.4.2.23`                                 |
| `13`     | `IBSS DFS`                                              | `IBSS DFS`                      | `IBSS DFS`元素包含在 IBSS 中进行动态频率选择(`DFS`)操作所需的信息 | `9.4.2.24`                                 |
| `14`     | `TPC Report`                                            | `TPC`报告                       | `TPC Report`元素包含在响应`TPC`请求元素或链路测量请求帧时发送的发射功率和链路余量信息 | `9.4.2.17`                                 |
| `15`     | `ERP`                                                   | `ERP`                           | `ERP`元素包含有关`BSS`中存在不支持`Clause 18`(`ERP-OFDM`)数据速率的`Clause 15`或`Clause 16 STA`的信息。此外，它还包含`ERP`元素发送方(`AP`、`IBSS STA`或`mesh STA`)对保护机制使用的要求，以优化`BSS`性能，以及对使用长或短`Barker`前导码的要求 | `9.4.2.12`                                 |
| `16`     | `Extended Supported Rates and BSS Membership Selectors` | 扩展支持的速率与`BSS`成员选择器 | 列出额外的速率，还可以包含`BSS`成员资格选择器(`BSS Membership Selectors`) | `9.4.2.13`                                 |
| `17`     | `RSN`                                                   | 强健安全网络                    | 一种安全网络，仅允许建立强健安全网络关联(`RSNA`)             | `P162`                                     |
| `18`     | `BSS Load`                                              | `BSS`负载                       | `BSS Load`元素包含有关`BSS`中当前`STA`数量和流量水平的信息   | `9.4.2.28`                                 |
| `19`     | `EDCA Parameter Set`                                    | `EDCA`参数集                    | `EDCA Parameter Set`元素为`STA`在竞争周期(`CP`)内正确运行`QoS`功能提供了所需的信息 | `9.4.2.29`                                 |
| `20`     | `Measurement Pilot Transmission`                        | 测量导频传输                    | 测量导频传输值                                               | `9.4.2.42`                                 |
| `21`     | `Multiple BSSID`                                        | 多`BSSID`                       | 支持单个物理`AP`(主`BSSID`)广播多个逻辑`BSS`(即多个虚拟`AP`)，每个逻辑`BSS`拥有自己独立的`BSSID`和`SSID`(可选) | `9.4.2.46`                                 |
| `22`     | `RM Enabled Capabilities`                               | `RM`启用的能力                  | `RM Enabled Capabilities`元素用于表明`STA`对无线射频测量的支持 | `9.4.2.45`                                 |
| `23`     | `AP Channel Report`                                     | `AP`信道报告                    | `AP`信道报告元素包含一个信道列表，`STA`可在这些信道上找到`AP` | `9.4.2.36`                                 |
| `24`     | `BSS Average Access Delay`                              | `BSS`平均接入延迟               | `BSS`平均接入延迟元素包含`AP`平均接入延迟，该值用于衡量`BSS`中的负载情况 | `9.4.2.39`                                 |
| `25`     | `Antenna`                                               | 天线                            | `Antenna ID`字段包含相关天线的识别号码。天线`ID`标识了用于传输包含该天线元素帧的天线 | `9.4.2.40`                                 |
| `26`     | `BSS Available Admission Capacity`                      | `BSS`可用准入容量               | `BSS`可用准入容量元素包含一个列表，列出了不同用户优先级(`User Priorities`)和接入类别(`Access Categories`)下的可用准入容量字段 | `9.4.2.43`                                 |
| `27`     | `BSS AC Access Delay`                                   | `BSS AC`接入延迟                | `BSS AC`接入延迟元素包含一个接入类别接入延迟(`Access Category Access Delay`)字段，`AC`接入延迟是`QoS AP`对于每个指定接入类别的服务的平均接入延迟的标量指示 | `9.4.2.44`                                 |
| `28`     | `Mobility Domain`                                       | 移动域                          | `Mobility Domain`元素(`MDE`)包含移动域标识符(`MDID`)字段以及快速切换(`FT`)能力与策略(`FT Capability and Policy`)字段。`AP`使用`MDE`来声明其属于构成某一移动域的`AP`组，通告其对`FT`能力的支持，并发布其`FT`策略信息 | `9.4.2.47`                                 |
| `29`     | `DSE registered location`                               | `DSE`注册位置                   | `DSE`注册位置元素包含`DSE`位置配置信息(`LCI`)，其中包括纬度、经度和海拔信息 | `9.4.2.52`                                 |
| `30`     | `Extended Channel Switch Announcement`                  | 扩展信道切换公                  | 用于通告`BSS`即将切换到一个新信道，或切换到新运行类别(`operating class`)中的一个新信道 | `9.4.2.53`                                 |
| `31`     | `Supported Operating Classes`                           | 支持的操作类别                  | `Supported Operating Classes`元素由`STA`用于通告其在本国所支持的操作类别 | `9.4.2.54`                                 |
| `32`     | `HT Capabilities`                                       | `HT`能力                        | `HT STA`通过发送`HT Capabilities`元素来声明其为`HT`站点      | `9.4.2.56`                                 |
| `33`     | `HT Operation`                                          | `HT`操作                        | `BSS`中`HT`站点的操作由`HT`操作元素控制                      | `9.4.2.57`                                 |
| `34`     | `20/40 BSS Coexistence`                                 | `20/40 BSS`共存                 | `20/40 BSS Coexistence`元素被`STA`用于交换影响`20/40 MHz BSS`共存的信息。这一元素帮助`STA`之间就共存于同一频段内的操作参数进行信息交流，确保网络性能和稳定性，尤其是在同时支持`20 MHz`和`40 MHz`信道带宽的情况下 | `9.4.2.60`                                 |
| `35`     | `Overlapping BSS Scan Parameters`                       | 重叠`BSS`扫描参数               | 重叠`BSS`扫描参数元素由`AP`使用，用于指示`BSS`成员在执行`OBSS`扫描操作时应使用的值 | `9.4.2.59`                                 |
| `36`     | `Extended Capabilities`                                 | 扩展能力                        | `Extended Capabilities`元素携带有关`STA`能力的信息，这些信息是对`Capability Information field`中指定能力的补充 | `9.4.2.27`                                 |
| `37`     | `QoS Traffic Capability`                                | `QoS`业务能力                   | `QoS Traffic Capability`元素提供了由非`AP QoS STA`产生的业务类型的信息，并被`QoS AP`用来指示关联的非`AP QoS STA`的访问类别 | `9.4.2.78`                                 |
| `38`     | `Channel Usage`                                         | 信道使用情况                    | 信道使用情况元素定义了非基础设施型`BSS`或非`TDLS`异频直连链路的 BSS 的信道使用信息 | `9.4.2.86`                                 |
| `39`     | `Time Advertisement`                                    | 时间通告                        | 用于描述与某一时间标准相对应的时间源、外部时钟(即外部时间源)、该时间标准与`TSF`计时器之间偏移量的估计值，以及该偏移量估计误差的标准差估计值 | `9.4.2.61`                                 |
| `40`     | `Time Zone`                                             | 时区                            | `Time Zone`元素包含`AP`所在的本地时区信息                    | `9.4.2.87`                                 |
| `41`     | `Interworking`                                          | 互通功能                        | `Interworking`元素包含了`STA`的互通服务能力的信息            | `9.4.2.92`                                 |
| `42`     | `Advertisement Protocol`                                | 通告协议                        | `Advertisement Protocol`元素包含识别特定通告协议及其相应通告控制的信息 | `9.4.2.93`                                 |
| `43`     | `Roaming Consortium`                                    | 漫游联盟                        | `Roaming Consortium`元素包含用于标识漫游联盟或`SSP`(服务提供商)的信息，`STA`可使用该联盟或`SSP`的安全凭证与发送此元素的`AP`进行认证 | `9.4.2.96`                                 |
| `44`     | `Emergency Alert Identifier`                            | 紧急警报标识符                  | `Emergency Alert Identifier`元素提供一个哈希值，用于识别当前网络中可用的活跃`EAS`(紧急警报系统)消息实例。该哈希值使非`AP STA`能够判断`AP`所通告的`EAS`消息是否已被先前接收过，从而决定是否有必要从网络下载该消息 | `9.4.2.97`                                 |
| `45`     | `Mesh ID`                                               | `Mesh ID`                       | `Mesh ID`元素用于通告`MBSS`(Mesh 基本服务集)的标识           | `9.4.2.99`                                 |
| `46`     | `Mesh Configuration`                                    | `Mesh`配置                      | `Mesh Configuration`元素用于通告`Mesh`服务                   | `9.4.2.98`                                 |
| `47`     | `Mesh Awake Window`                                     | `Mesh`唤醒窗口                  | `Mesh Awake Window`元素出现在`DTIM`信标帧中，并可选地出现在信标帧和探测响应帧中 | `9.4.2.104`                                |
| `48`     | `Beacon Timing`                                         | 信标定时                        | `Beacon Timing`元素用于通告邻近`STA`(包括`Mesh STA`、`IBSS AP`或`IBSS STA`)的信标定时信息 | `9.4.2.105`                                |
| `49`     | `MCCAOP Advertisement Overview`                         | `MCCAOP`通告概览                | `MCCAOP Advertisement Overview`元素由`Mesh STA`使用，用于向其邻居通告自身的`MCCA`信息以及其`MCCAOP`通告元素的相关信息，从而表示其`MCCAOP`通告集合 | `9.4.2.108`                                |
| `50`     | `MCCAOP Advertisement`                                  | `MCCAOP`通告                    | `MCCAOP Advertisement`元素由`Mesh STA`使用，用于向其邻居通告`MCCAOP`预约信息 | `9.4.2.109`                                |
| `51`     | `Mesh Channel Switch Parameters`                        | `Mesh`信道切换参数              | `Mesh Channel Switch Parameters`元素由`Mesh STA`与信道切换公告(`Channel Switch Announcement`)元素及扩展信道切换公告(`Extended Channel Switch Announcement`)元素配合使用，用于向其他`Mesh STA`通告其即将切换到新的工作信道和/或新的运行类别 | `9.4.2.103`                                |
| `52`     | `QMF Policy`                                            | `QMF`策略                       | `QMF`策略帧采用动作帧(`Action frame`)格式，由请求`STA`发送给接收站点，并包含所附带的`QMF`策略 | `9.6.8.18`                                 |
| `53`     | `QLoad Report`                                          | `QLoad`报告                     | `QLoad Report`元素包含支持重叠基本服务集(`OBSS`)管理所需的一组参数 | `9.4.2.123`                                |
| `54`     | `Multi-band`                                            | 多频段                          | `Multi-band`元素表明发送该元素的`STA`位于一个多频段设备中，该设备能够在不同于当前传输该元素所用频段、运行类别或信道的其他频段、运行类别或信道上工作 | `9.4.2.138`                                |
| `55`     | `DMG Capabilities`                                      | `DMG`能力                       | `DMG Capabilities`元素包含一个`STA`标识符以及若干字段，用于通告`DMG STA`对可选`DMG`功能的支持情况 | `9.4.2.128`                                |
| `56`     | `DMG Operation`                                         | `DMG`操作                       | 由`AP`或`PCP`提供的`BSS`运行参数                             | `9.4.2.129`                                |
| `57`     | `Multiple MAC Sublayers`                                | 多个`MAC`子层                   | 本设备在多链路操作中，是否为每个链路使用 独立的`MAC`子层实例 | `9.4.2.153`                                |
| `58`     | `Antenna Sector ID Pattern`                             | 天线扇区`ID`模式                | 主要用于 定向波束成形(`Directional Beamforming`) 和 扇区级扫描(`Sector Level Sweep, SLS`) 过程中，以高效建立毫米波(`mmWave`)链路 | `9.4.2.157`                                |
| `59`     | `VHT Capabilities`                                      | `VHT`能力                       | `VHT Capabilities`元素声明它是一个`VHT STA`。`VHT`能力元素包含了若干字段，用于通告`VHT STA`的`VHT`能力 | `9.4.2.158`                                |
| `60`     | `VHT Operation`                                         | 运行模式通知                    | `BSS`中`VHT`站点的操作由`HT`操作元素和`VHT`操作元素共同控制  | `9.4.2.159`                                |
| `61`     | `Transmit Power Envelope element`                       | 发射功率包络元素                | `Transmit Power Envelope`元素传达了不同传输带宽下的本地最大传输功率 | `9.4.2.162`                                |
| `62`     | `Channel Switch Wrapper element`                        | 信道切换封装元素                | `Channel Switch Wrapper`元素包含了一些子元素，这些子元素指示了信道切换后`BSS`的特性 | `9.4.2.163`                                |
| `63`     | `Extended BSS Load element`                             | 扩展`BSS`负载元素               | `Extended BSS Load`元素包含有关`MIMO`空间流未充分利用情况和带宽利用率的信息 | `9.4.2.160`                                |
| `64`     | `Quiet Channel`                                         | 静默信道                        | `Quiet Channel`元素用于指示在`Quiet`间隔期间，`VHT BSS`的次级`80 MHz`信道应保持静默 | `9.4.2.165`                                |
| `65`     | `Operating Mode Notification`                           | 操作模式通知                    | `Operating Mode Notification`元素用于通知其他`STA`：发送该元素的STA正在更改其一个或多个操作参数，包括操作信道带宽、可接收的最大空间流数量以及`LDPC`接收偏好 | `9.4.2.166、9.6.23.4`                      |
| `66`     | `Reduced Neighbor Report`                               | 简化邻居报告                    | `Reduced Neighbor Report`元素包含与邻近`AP`相关的信道及其他信息 | `9.4.2.171`                                |
| `67`     | `TVHT Operation`                                        | `TVHT`运行                      | `BSS`中`TVHT STA`的操作由`TVHT`操作(`TVHT Operation`)元素控制 | `9.4.2.172`                                |
| `68`     | `Estimated Service Parameters`                          | 预估服务参数                    | 预估服务参数元素由一个`STA`用来向另一个`STA`提供信息，后者可将该信息作为算法的输入，以估算两个`STA`之间的吞吐量 | `9.4.2.174`                                |
| `69`     | `Relay Capabilities`                                    | 中继能力                        | 打算参与中继操作的`STA`通过中继能力(`Relay Capabilities`)元素来通告其能力 | `9.4.2.148`                                |
| `Last-1` | `Vendor Specific`                                       | 厂商特定信息                    | `Vendor Specific`元素用于在单一定义的格式内承载本标准中未定义的信息，以避免为非标准用途占用保留的元素标识符，并使得在存在非标准信息的情况下更容易实现互操作性 | `9.4.2.26`                                 |
| `Last`   | `Requested elements`                                    | 请求的元素                      | 探测请求帧(`Probe Request frame`)中的请求元素(`Request element`)或扩展请求元素(`Extended Request element`)所请求的元素 | `6.3.3.3.2`                                |

## 时间通告帧(Timing Advertisement)

`Timing Advertisement`帧是`IEEE 802.1AS`标准中定义的一种特殊以太网帧，主要用于时间敏感网络(`TSN, Time-Sensitive Networking`)和精确时间协议(`PTP, Precision Time Protocol`)环境中。它在基于`IEEE 1588`的时间同步机制基础上，为`TSN`网络提供更高效、低开销的时间同步支持

### Timing Advertisement帧主体

> 笔者注：Timing Advertisement帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.16**

| 顺序   | 信息                     | 中文翻译     | 概述                                                         | IEEE 802.11-2016文档详细解释对应章节或位置 |
| ------ | ------------------------ | ------------ | ------------------------------------------------------------ | ------------------------------------------ |
| `1`    | `Timestamp`              | 时间戳       | 帧源端的定时同步功能(`TSF`)计时器的值                        | `9.4.1.10`                                 |
| `2`    | `Capability Information` | 能力信息     | `Capability Information`字段包含若干子字段，用于指示所请求或公布的可选能力 | `9.4.1.4`                                  |
| 3      | `Country`                | 国家信息     | `Country`元素包含允许`STA`识别其所在监管域并配置其物理层以在该监管域内操作所需的信息 | `9.4.2.9`                                  |
| `4`    | `Power Constraint`       | 功率限制     | 功率限制元素包含必要的信息，用于使`STA`能够确定当前信道中的本地最大发射功率 | `9.4.2.14`                                 |
| `5`    | `Time Advertisement`     | 时间通告     | 用于描述与某一时间标准相对应的时间源、外部时钟(即外部时间源)、该时间标准与`TSF`计时器之间偏移量的估计值，以及该偏移量估计误差的标准差估计值 | `9.4.2.61`                                 |
| `6`    | `Extended Capabilities`  | 扩展能力     | `Extended Capabilities`元素携带有关`STA`能力的信息，这些信息是对`Capability Information field`中指定能力的补充 | `9.4.2.27`                                 |
| `Last` | `Vendor specific`        | 厂商特定信息 | `Vendor Specific`元素用于在单一定义的格式内承载本标准中未定义的信息，以避免为非标准用途占用保留的元素标识符，并使得在存在非标准信息的情况下更容易实现互操作性 | `9.4.2.26`                                 |

## 信标帧(Beacon)

信标帧是最重要的`802.11`帧类型之一，又称信标管理帧，它相当于无线网络的心跳消息。在基本服务集中，接入点广播信标帧，而客户端侦听信标帧；信标帧包含客户端在加入基本服务集前需要了解的所有重要信息。信标帧每隔`102.4`毫秒(目标时间)发送一次，这意味着接入点大约每秒发送`10`次信标帧。接入点可以调整信标间隔，但是无法禁用这个参数。大部分情况下，增加信标间隔可能对客户端连接性能造成不利影响，接入点利用信标帧向客户端通告基本服务集的所有配置功能。如果接入点配置有多个服务集标识符(`SSID`),它将向每个`SSID`发送信标帧

客户端必须首先发现接入点才能进行连接，可以通过**侦听接入点(被动扫描)或者搜索接入点(主动扫描)来发现接入点**

### Beacon帧主体

> 笔者注：Beacon帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.3**

| 顺序   | 信息                                                    | 中文翻译                        | 概述                                                         | IEEE 802.11-2016文档详细解释对应章节或位置 |
| ------ | ------------------------------------------------------- | ------------------------------- | ------------------------------------------------------------ | ------------------------------------------ |
| `1`    | `Timestamp`                                             | 时间戳                          | 帧源端的定时同步功能(`TSF`)计时器的值                        | `9.4.1.10`                                 |
| `2`    | `Beacon interval`                                       | 信标间隔                        | 目标信标传输时间(`TBTT`)之间的时间单位(`TU`)数量             | `9.4.1.3`                                  |
| `3`    | `Capability Information`                                | 能力信息                        | `Capability Information`字段包含若干子字段，用于指示所请求或公布的可选能力 | `9.4.1.4`                                  |
| `4`    | `Service Set Identifier(SSID)`                          | 服务集标识符                    | `SSID`元素用于标识一个`ESS`(扩展服务集)或`IBSS`(独立基本服务集)的标识信息 | `9.4.2.2`                                  |
| `5`    | `Supported Rates and BSS Membership Selectors`          | 支持的速率与`BSS`成员选择器     | 声明支持的传输速率和加入`BSS`的能力门槛                      | `9.4.2.3`                                  |
| `6`    | `DSSS Parameter Set`                                    | `DSSS`参数集                    | `DSSS`参数集元素包含用于使`STA`能够识别信道号的信息          | `9.4.2.4`                                  |
| `7`    | `CF Parameter Set`                                      | `CF`参数集                      | `CF`参数集元素包含支持`PCF`所需的一组参数                    | `9.4.2.5`                                  |
| `8`    | `IBSS Parameter Set`                                    | `IBSS`参数集                    | `IBSS`参数集元素包含支持`IBSS`所需的一组参数                 | `9.4.2.7`                                  |
| `9`    | `Traffic indication map(TIM)`                           | 流量指示映射                    | `Traffic Indication Map`用于省电管理，由`AP`用来告知处于休眠状态的关联`STA`是否有为其缓存的下行数据 | `9.4.2.6`                                  |
| `10`   | `Country`                                               | 国家信息                        | `Country`元素包含允许`STA`识别其所在监管域并配置其物理层以在该监管域内操作所需的信息 | `9.4.2.9`                                  |
| `11`   | `Power Constraint`                                      | 功率限制                        | 功率限制元素包含必要的信息，用于使`STA`能够确定当前信道中的本地最大发射功率 | `9.4.2.14`                                 |
| `12`   | `Channel Switch Announcement`                           | 信道切换公告                    | `Channel Switch Announcement`元素用于通告其即将切换到的新信道及新信道的信道号 | `9.4.2.19`                                 |
| `13`   | `Quiet`                                                 | 静默                            | `Quiet`元素定义了当前信道中不进行任何传输的时间间隔。该时间间隔可用于辅助进行信道测量，避免受到同一`BSS`中其他`STA`的干扰 | `9.4.2.23`                                 |
| `14`   | `IBSS DFS`                                              | `IBSS DFS`                      | `IBSS DFS`元素包含在 IBSS 中进行动态频率选择(`DFS`)操作所需的信息 | `9.4.2.24`                                 |
| `15`   | `TPC Report`                                            | `TPC`报告                       | `TPC Report`元素包含在响应`TPC`请求元素或链路测量请求帧时发送的发射功率和链路余量信息 | `9.4.2.17`                                 |
| `16`   | `ERP`                                                   | `ERP`                           | `ERP`元素包含有关`BSS`中存在不支持`Clause 18(ERP-OFDM)`数据速率的`Clause 15`或`Clause 16 STA`的信息。此外，它还包含`ERP`元素发送方(`AP`、`IBSS STA`或`mesh STA`)对保护机制使用的要求，以优化`BSS`性能，以及对使用长或短`Barker`前导码的要求 | `9.4.2.12`                                 |
| `17`   | `Extended Supported Rates and BSS Membership Selectors` | 扩展支持的速率与`BSS`成员选择器 | 列出额外的速率，还可以包含 BSS 成员资格选择器(`BSS Membership Selectors`) | `9.4.2.13`                                 |
| `18`   | `RSN`                                                   | 强健安全网络                    | 一种安全网络，仅允许建立强健安全网络关联(`RSNA`)             | `P162`                                     |
| `19`   | `BSS Load`                                              | `BSS`负载                       | `BSS Load`元素包含有关`BSS`中当前`STA`数量和流量水平的信息   | `9.4.2.28`                                 |
| `20`   | `EDCA Parameter Set`                                    | `EDCA`参数集                    | `EDCA Parameter Set`元素为`STA`在竞争周期(`CP`)内正确运行`QoS`功能提供了所需的信息 | `9.4.2.29`                                 |
| `21`   | `QoS Capability`                                        | `QoS`能力                       | `QoS Capability`元素包含多个子字段，用于在支持`QoS`的站点(`QoS STA`)中通告其可选的`QoS`功能 | `9.4.2.35`                                 |
| `22`   | `AP Channel Report`                                     | `AP`信道报告                    | `AP`信道报告元素包含一个信道列表，`STA`可在这些信道上找到`AP` | `9.4.2.36`                                 |
| `23`   | `BSS Average Access Delay`                              | `BSS`平均接入延迟               | `BSS`平均接入延迟元素包含`AP`平均接入延迟，该值用于衡量`BSS`中的负载情况 | `9.4.2.39`                                 |
| `24`   | `Antenna`                                               | 天线                            | `Antenna ID`字段包含相关天线的识别号码。天线`ID`标识了用于传输包含该天线元素帧的天线 | `9.4.2.40`                                 |
| `25`   | `BSS Available Admission Capacity`                      | `BSS`可用准入容量               | `BSS`可用准入容量元素包含一个列表，列出了不同用户优先级(`User Priorities`)和接入类别(`Access Categories`)下的可用准入容量字段 | `9.4.2.43`                                 |
| `26`   | `BSS AC Access Delay`                                   | `BSS AC`接入延迟                | `BSS AC`接入延迟元素包含一个接入类别接入延迟(`Access Category Access Delay`)字段，`AC`接入延迟是`QoS AP`对于每个指定接入类别的服务的平均接入延迟的标量指示 | `9.4.2.44`                                 |
| `27`   | `Measurement Pilot Transmission`                        | 测量导频传输                    | 测量导频传输值                                               | `9.4.2.42`                                 |
| `28`   | `Multiple BSSID`                                        | 多`BSSID`                       | 支持单个物理`AP`(主`BSSID`)广播多个逻辑`BSS`(即多个虚拟`AP`)，每个逻辑`BSS`拥有自己独立的`BSSID`和`SSID`(可选) | `9.4.2.46`                                 |
| `29`   | `RM Enabled Capabilities`                               | `RM`启用的能力                  | `RM Enabled Capabilities`元素用于表明`STA`对无线射频测量的支持 | `9.4.2.45`                                 |
| `30`   | `Mobility Domain`                                       | 移动域                          | `Mobility Domain`元素(`MDE`)包含移动域标识符(`MDID`)字段以及快速切换(`FT`)能力与策略(`FT Capability and Policy`)字段。`AP`使用`MDE`来声明其属于构成某一移动域的`AP`组，通告其对`FT`能力的支持，并发布其`FT`策略信息 | `9.4.2.47`                                 |
| `31`   | `DSE registered location`                               | `DSE`注册位置                   | `DSE`注册位置元素包含`DSE`位置配置信息(`LCI`)，其中包括纬度、经度和海拔信息 | `9.4.2.52`                                 |
| `32`   | `Extended Channel Switch Announcement`                  | 扩展信道切换公                  | 用于通告`BSS`即将切换到一个新信道，或切换到新运行类别(`operating class`)中的一个新信道 | `9.4.2.53`                                 |
| `33`   | `Supported Operating Classes`                           | 支持的操作类别                  | `Supported Operating Classes`元素由`STA`用于通告其在本国所支持的操作类别 | `9.4.2.54`                                 |
| `34`   | `HT Capabilities`                                       | `HT`能力                        | `HT STA`通过发送`HT Capabilities`元素来声明其为`HT`站点      | `9.4.2.56`                                 |
| `35`   | `HT Operation`                                          | `HT`操作                        | `BSS`中`HT`站点的操作由`HT`操作元素控制                      | `9.4.2.57`                                 |
| `36`   | `20/40 BSS Coexistence`                                 | `20/40 BS`S共存                 | `20/40 BSS Coexistence`元素被`STA`用于交换影响`20/40 MHz BSS`共存的信息。这一元素帮助`STA`之间就共存于同一频段内的操作参数进行信息交流，确保网络性能和稳定性，尤其是在同时支持`20 MHz`和`40 MHz`信道带宽的情况下 | `9.4.2.60`                                 |
| `37`   | `Overlapping BSS Scan Parameters`                       | 重叠`BSS`扫描参数               | 重叠`BSS`扫描参数元素由`AP`使用，用于指示BSS成员在执行`OBSS`扫描操作时应使用的值 | `9.4.2.59`                                 |
| `38`   | `Extended Capabilities`                                 | 扩展能力                        | `Extended Capabilities`元素携带有关`STA`能力的信息，这些信息是对`Capability Information field`中指定能力的补充 | `9.4.2.27`                                 |
| `39`   | `FMS Descriptor`                                        | `FMS`描述符                     | 定义有关`AP`中缓存的组播业务单元(`BUs`)的信息                | `9.4.2.75`                                 |
| `40`   | `QoS Traffic Capability`                                | `QoS`业务能力                   | `QoS Traffic Capability`元素提供了由非`AP QoS STA`产生的业务类型的信息，并被`QoS AP`用来指示关联的非`AP QoS STA`的访问类别 | `9.4.2.78`                                 |
| `41`   | `Time Advertisement`                                    | 时间通告                        | 用于描述与某一时间标准相对应的时间源、外部时钟(即外部时间源)、该时间标准与`TSF`计时器之间偏移量的估计值，以及该偏移量估计误差的标准差估计值 | `9.4.2.61`                                 |
| `42`   | `Interworking`                                          | 互通功能                        | `Interworking`元素包含了`STA`的互通服务能力的信息            | `9.4.2.92`                                 |
| `43`   | `Advertisement Protocol`                                | 通告协议                        | `Advertisement Protocol`元素包含识别特定通告协议及其相应通告控制的信息 | `9.4.2.93`                                 |
| `44`   | `Roaming Consortium`                                    | 漫游联盟                        | `Roaming Consortium`元素包含用于标识漫游联盟或`SSP`(服务提供商)的信息，`STA`可使用该联盟或`SSP`的安全凭证与发送此元素的`AP`进行认证 | `9.4.2.96`                                 |
| `45`   | `Emergency Alert Identifier`                            | 紧急警报标识符                  | `Emergency Alert Identifier`元素提供一个哈希值，用于识别当前网络中可用的活跃`EAS`(紧急警报系统)消息实例。该哈希值使非`AP STA`能够判断`AP`所通告的`EAS`消息是否已被先前接收过，从而决定是否有必要从网络下载该消息 | `9.4.2.97`                                 |
| `46`   | `Mesh ID`                                               | `Mesh ID`                       | `Mesh ID`元素用于通告`MBSS`(`Mesh`基本服务集)的标识          | `9.4.2.99`                                 |
| `47`   | `Mesh Configuration`                                    | `Mesh`配置                      | `Mesh Configuration`元素用于通告`Mesh`服务                   | `9.4.2.98`                                 |
| `48`   | `Mesh Awake Window`                                     | `Mesh`唤醒窗口                  | `Mesh Awake Window`元素出现在`DTIM`信标帧中，并可选地出现在信标帧和探测响应帧中 | `9.4.2.104`                                |
| `49`   | `Beacon Timing`                                         | 信标定时                        | `Beacon Timing`元素用于通告邻近`STA`(包括`Mesh STA`、`IBSS AP`或`IBSS STA`)的信标定时信息 | `9.4.2.105`                                |
| `50`   | `MCCAOP Advertisement Overview`                         | `MCCAOP`通告概览                | `MCCAOP Advertisement Overview`元素由`Mesh STA`使用，用于向其邻居通告自身的`MCCA`信息以及其`MCCAOP`通告元素的相关信息，从而表示其`MCCAOP`通告集合 | `9.4.2.108`                                |
| `51`   | `MCCAOP Advertisement`                                  | `MCCAOP`通告                    | `MCCAOP Advertisement`元素由`Mesh STA`使用，用于向其邻居通告`MCCAOP`预约信息 | `9.4.2.109`                                |
| `52`   | `Mesh Channel Switch Parameters`                        | `Mesh`信道切换参数              | `Mesh Channel Switch Parameters`元素由`Mesh STA`与信道切换公告(`Channel Switch Announcement`)元素及扩展信道切换公告(`Extended Channel Switch Announcement`)元素配合使用，用于向其他`Mesh STA`通告其即将切换到新的工作信道和/或新的运行类别 | `9.4.2.103`                                |
| `53`   | `QMF Policy`                                            | `QMF`策略                       | `QMF`策略帧采用动作帧(`Action frame`)格式，由请求`STA`发送给接收站点，并包含所附带的`QMF`策略 | `9.6.8.18`                                 |
| `54`   | `QLoad Report`                                          | `QLoad`报告                     | `QLoad Report`元素包含支持重叠基本服务集(`OBSS`)管理所需的一组参数 | `9.4.2.123`                                |
| `55`   | `HCCA TXOP Update Count`                                | `HCCA TXOP`更新计数             | `HCCA TXOP`更新计数元素由接入点(`AP`)使用，以通告其`TXOP`调度的变化 | `9.4.2.124`                                |
| `56`   | `Multi-band`                                            | 多频段                          | `Multi-band`元素表明发送该元素的`STA`位于一个多频段设备中，该设备能够在不同于当前传输该元素所用频段、运行类别或信道的其他频段、运行类别或信道上工作 | `9.4.2.138`                                |
| `57`   | `VHT Capabilities`                                      | `VHT`能力                       | `VHT Capabilities`元素声明它是一个`VHT STA`。`VHT`能力元素包含了若干字段，用于通告`VHT STA`的`VHT`能力 | `9.4.2.158`                                |
| `58`   | `VHT Operation`                                         | 运行模式通知                    | `BSS`中`VHT`站点的操作由`HT`操作元素和`VHT`操作元素共同控制  | `9.4.2.159`                                |
| `59`   | `Transmit Power Envelope element`                       | 发射功率包络元素                | `Transmit Power Envelope`元素传达了不同传输带宽下的本地最大传输功率 | `9.4.2.162`                                |
| `60`   | `Channel Switch Wrapper element`                        | 信道切换封装元素                | `Channel Switch Wrapper`元素包含了一些子元素，这些子元素指示了信道切换后`BSS`的特性 | `9.4.2.163`                                |
| `61`   | `Extended BSS Load element`                             | 扩展`BSS`负载元素               | `Extended BSS Load`元素包含有关`MIMO`空间流未充分利用情况和带宽利用率的信息 | `9.4.2.160`                                |
| `62`   | `Quiet Channel`                                         | 静默信道                        | `Quiet Channel`元素用于指示在`Quiet`间隔期间，`VHT BSS`的次级`80 MHz`信道应保持静默 | `9.4.2.165`                                |
| `63`   | `Operating Mode Notification`                           | 操作模式通知                    | `Operating Mode Notification`元素用于通知其他`STA`：发送该元素的`STA`正在更改其一个或多个操作参数，包括操作信道带宽、可接收的最大空间流数量以及`LDPC`接收偏好 | `9.4.2.166、9.6.23.4`                      |
| `64`   | `Reduced Neighbor Report`                               | 简化邻居报告                    | `Reduced Neighbor Report`元素包含与邻近`AP`相关的信道及其他信息 | `9.4.2.171`                                |
| `65`   | `TVHT Operation`                                        | `TVHT`运行                      | `BSS`中`TVHT STA`的操作由`TVHT`操作(`TVHT Operation`)元素控制 | `9.4.2.172`                                |
| `66`   | `Estimated Service Parameters`                          | 预估服务参数                    | 预估服务参数元素由一个`STA`用来向另一个`STA`提供信息，后者可将该信息作为算法的输入，以估算两个`STA`之间的吞吐量 | `9.4.2.174`                                |
| `67`   | `Future Channel Guidance`                               | 未来信道指引                    | 未来信道指引元素由`AP`、`IBSS`站点、网格站点或`PCP`使用，用于在发送`STA`更改操作信道的情况下，指导其他`STA`关于可能的未来信道的信息 | `9.4.2.175`                                |
| `Last` | `Vendor Specific`                                       | 厂商特定信息                    | `Vendor Specific`元素用于在单一定义的格式内承载本标准中未定义的信息，以避免为非标准用途占用保留的元素标识符，并使得在存在非标准信息的情况下更容易实现互操作性 | `9.4.2.26`                                 |

## 通知传输指示消息帧(ATIM)

`ATIM`帧专门用于`IBSS`网络中，协调处于节能模式的`STA`之间的通信

### ATIM帧主体

`ATIM`帧的帧体为空；详情见**IEEE 802.11-2016文档9.3.3.4** 

## 解除关联帧(Disassociation)

取消关联帧属于通知而非请求，如果客户端希望取消与接入点的关联，或接入点希望取消与客户端的关联，将向对方发送取消关联帧。解除关联帧可以由`AP`主动发送，也可以由`STA`主动发送，当对方不适合加入网络时，工作站也会送出`Disassociation`(解除连接)或`Deauthentication`(解除身份认证)帧作为应答

### Disassociation帧主体

> 笔者注：Disassociation帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.5**

| 顺序     | 信息                                                         | 中文翻译                                                     | 概述                                                         | IEEE 802.11-2016文档详细解释对应章节或位置 |
| -------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------ |
| `1`      | `Reason code`                                                | 响应代码                                                     | `Reason Code`字段用于指示生成类型为解除关联(`Disassociation`)、解除认证(`Deauthentication`)、`DELTS`、`DELBA`、`DLS`拆除(`DLS Teardown`)或`Mesh`对等关闭(`Mesh Peering Close`)的未经请求的通知管理帧的原因 | `9.4.1.7`                                  |
| `Last-1` | `One or more vendor-specific elements are optionally present.` | 一个或多个供应商特定元素可选地存在                           | `Vendor Specific`元素用于在单一定义的格式内承载本标准中未定义的信息，以避免为非标准用途占用保留的元素标识符，并使得在存在非标准信息的情况下更容易实现互操作性 | `9.4.2.26`                                 |
| `Last`   | `The Management MIC element(MME)is present when management frame protection is enabled at the AP and the frame is a group addressed frame` | 当在`AP`处启用了管理帧保护并且该帧是组地址帧时，管理`MIC`元素(`MME`)存在 | `MIC`元素为网状互联管理帧提供了消息完整性保障                | `9.4.2.119`                                |

## 身份认证帧(Authentication)

在`802.11`客户端通过接入点向网络中的其他设备传输流量前，必须按顺序验证身份并建立关联。这种身份认证类似于将以太网电缆插入墙壁插孔，`802.11`身份验证只是为了客户端与接入点之间建立初步连接，相当于验证两台设备是否属于有效的`802.11`设备

`802.11`原始标准定义了两种不同的身份验证机制

1. **共享密钥身份验证**

   共享密钥身份验证采用`WEP`验证客户端的身份，客户端和接入点都要配置静态`WEP`密钥。**现在WEP早已过时，共享密钥身份验证也不再使用**

2. **开放系统身份验证**

   开放系统身份验证不对客户端的身份做任何验证，本质上属于客户端与接入点之间的问候交换过程

### Authentication帧主体

> 笔者注：Authentication帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.12**

| 顺序   | 信息                                         | 中文翻译                 | 概述                                                         | IEEE 802.11-2016文档详细解释对应章节或位置 |
| ------ | -------------------------------------------- | ------------------------ | ------------------------------------------------------------ | ------------------------------------------ |
| `1`    | `Authentication algorithm number`            | 认证算法编号             | `Authentication Algorithm Number`字段用于指示一种认证算法    | `9.4.1.1`                                  |
| `2`    | `Authentication transaction sequence number` | 认证事务序列号           | `Authentication Transaction Sequence Number`字段用于指示多步骤事务当前的进展状态 | `9.4.1.2`                                  |
| `3`    | `Status code`                                | 状态码                   | 状态码字段用于响应管理帧中，以指示所请求操作的成功或失败     | `9.4.1.9`                                  |
| `4`    | `Challenge text`                             | 挑战文本                 | `Challenge Text`元素包含在认证交换过程中使用的挑战文本       | `9.4.2.8`                                  |
| `5`    | `RSN`                                        | 强健安全网络             | 一种安全网络，仅允许建立强健安全网络关联(`RSNA`)             | `P162`                                     |
| `6`    | `Mobility Domain`                            | 移动域                   | `Mobility Domain`元素(`MDE`)包含移动域标识符(`MDID`)字段以及快速切换(`FT`)能力与策略(`FT Capability and Policy`)字段。`AP`使用`MDE`来声明其属于构成某一移动域的`AP`组，通告其对`FT`能力的支持，并发布其`FT`策略信息 | `9.4.2.47`                                 |
| `7`    | `Fast BSS Transition`                        | 快速`BSS`切换            | 快速切换元素(`FTE`)包含在强健安全网络(`RSN`)中执行快速`BSS`切换期间所需的FT认证序列信息 | `9.4.2.48`                                 |
| `8`    | `Timeout Interval (reassociation deadline)`  | 超时间隔(重关联截止时间) | `Timeout Interval`元素用于指定时间间隔和超时值               | `9.4.2.49`                                 |
| `9`    | `RIC`                                        | 资源信息容器             | 资源信息容器(`RIC`)指的是一组用于表达资源请求或响应的元素集合 | `13.11.2`                                  |
| `10`   | `Finite Cyclic Group`                        | 有限循环群               | `Finite Cyclic Group`在`SAE`中用于指示在`SAE`交换过程中使用哪个加密群组 | `9.4.1.43`                                 |
| `11`   | `Anti-Clogging Token`                        | 防阻塞令牌               | `Anti-Clogging Token`字段与`SAE`认证一起使用，以提供拒绝服务攻击的防护 | `9.4.1.39`                                 |
| `12`   | `Send-Confirm`                               | 发送确认                 | `Send-Confirm`字段与`SAE`认证一起使用，作为防重放计数器      | `9.4.1.38`                                 |
| `13`   | `Scalar`                                     | 标量                     | `Scalar`字段与`SAE`认证一起使用，以传输加密材料              | `9.4.1.40`                                 |
| `14`   | `Finite field element`                       | 有限域元素               | `FFE`字段与`SAE`认证一起使用，用于传输有限域中的一个元素     | `9.4.1.41`                                 |
| `15`   | `Confirm`                                    | 确认                     | `Confirm`字段与`SAE`认证一起使用，用于对加密密钥进行认证并证明其持有 | `9.4.1.42`                                 |
| `16`   | `Multi-band`                                 | 多频段                   | `Multi-band`元素表明发送该元素的`STA`位于一个多频段设备中，该设备能够在不同于当前传输该元素所用频段、运行类别或信道的其他频段、运行类别或信道上工作 | `9.4.2.138`                                |
| `17`   | `Neighbor Report`                            | 邻近报告                 | 帮助`STA`发现周围的`AP`信息，从而支持更智能的漫游和网络切换决策 | `9.4.2.37`                                 |
| `Last` | `Vendor Specific`                            | 厂商特定信息             | `Vendor Specific`元素用于在单一定义的格式内承载本标准中未定义的信息，以避免为非标准用途占用保留的元素标识符，并使得在存在非标准信息的情况下更容易实现互操作性 | `9.4.2.26`                                 |

## 解除身份认证帧(Deauthentication)

与取消关联类似，解除身份验证也属于通知而非请求。如果客户端希望解除与接入点的身份验证，或接入点希望解除与客户端的身份验证，将向对方发送解除身份验证帧。由于身份验证是建立关联的先决条件，因此解除身份验证会自动取消关联

### Deauthentication帧主体

> 笔者注：Deauthentication帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.13**

| 顺序     | 信息                                                         | 中文翻译                                                     | 概述                                                         | IEEE 802.11-2016文档详细解释对应章节或位置 |
| -------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------ |
| `1`      | `Reason code`                                                | 响应代码                                                     | `Reason Code`字段用于指示生成类型为解除关联(`Disassociation`)、解除认证(`Deauthentication`)、`DELTS`、`DELBA`、`DLS`拆除(`DLS Teardown`)或`Mesh`对等关闭(`Mesh Peering Close`)的未经请求的通知管理帧的原因 | `9.4.1.7`                                  |
| `Last-1` | `One or more vendor-specific elements are optionally present.` | 一个或多个供应商特定元素可选地存在                           | `Vendor Specific`元素用于在单一定义的格式内承载本标准中未定义的信息，以避免为非标准用途占用保留的元素标识符，并使得在存在非标准信息的情况下更容易实现互操作性 | `9.4.2.26`                                 |
| `Last`   | `The Management MIC element(MME)is present when management frame protection is enabled at the AP and the frame is a group addressed frame` | 当在`AP`处启用了管理帧保护并且该帧是组地址帧时，管理`MIC`元素(`MME`)存在 | `MIC`元素为网状互联管理帧提供了消息完整性保障                | `9.4.2.119`                                |

## 行动帧(Action)

`Action`帧用于在已关联的`STA`与`AP`之间，或`STA`与`STA`之间，**传递特定类别的控制或管理信息**

### Action帧主体

> 笔者注：Action帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.14**

| 顺序     | 信息                                                         | 中文翻译                                                     | 概述                                                         | IEEE 802.11-2016文档详细解释对应章节或位置 |
| -------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------ |
| `1`      | `Action`                                                     |                                                              | 长度为`1`字节，传递特定类别的控制或管理信息                  |                                            |
| `Last-2` | `One or more vendor-specific elements areoptionally present.These elements are absent when the Category subfield of the Action field is Vendor-Specific,Vendor-Specific Protected,or Self-protected or when the Category subfield of the Action field is VHT and the VHTAction subfield of the Action field is VHT Compressed Beamforming.` | 一个或多个供应商特定元素可选地存在。  当动作字段(`Action field`)的类别子字段(`Category subfield`)为“供应商特定(`Vendor-Specific`)”、“受保护的供应商特定(`Vendor-Specific Protected`)”或“自保护(`Self-protected`)”时，这些元素不存在； 或者，当动作字段的类别子字段为“`VHT`”，且动作字段的`VHTAction`子字段为“`VHT`压缩波束成形(`VHT Compressed Beamforming`)”时，这些元素也不存在。 | `Vendor Specific`元素用于在单一定义的格式内承载本标准中未定义的信息，以避免为非标准用途占用保留的元素标识符，并使得在存在非标准信息的情况下更容易实现互操作性 | `9.4.2.26`                                 |
| `Last-1` | `The Management MIC element(MME)is present when management frame protection is enabled at the AP,the frame is a group addressed robust Action frame,and the category of the Action frame does not support group addressed privacy as indicated by Table 9-47.` | 当在`AP`处启用了管理帧保护并且该帧是组地址帧时，管理`MIC`元素(`MME`)存在 | `MIC`元素为网状互联管理帧提供了消息完整性保障                | `9.4.2.119`                                |
| `Last`   | `The Authenticated Mesh Peering Exchange element is present in a Self-protected Action frame if dot11MeshSecurityActivated,dot11ProtectedQLoad eportActivated,or dot11ProtectedTXOPNegotiationActivated is true and a PMK exists between the sender and recipient of this frame,otherwise not present.` | 如果`dot11MeshSecurityActivated`、`dot11ProtectedQLoadReportActivated`或`dot11ProtectedTXOPNegotiationActivated`为真，并且在该帧的发送方与接收方之间已存在`PMK`(成对主密钥)，则在自保护动作帧(`Self-protected Action frame`)中包含“已认证`Mesh`对等交换(`Authenticated Mesh Peering Exchange`)”元素；否则，该元素不存在。 | /                                                            | /                                          |

## 无确认动作帧(Action No Ack)

### Action No Ack帧主体

> 笔者注：Action No Ack帧中的元素是否被使用取决于具体情况，**详情见IEEE 802.11-2016文档9.3.3.15**

| 顺序   | 信息                                                         | 中文翻译                                                     |
| ------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `1`    | `Action`                                                     |                                                              |
| `Last` | `Oneormorevendor-specific elements are optionally present. This element follows all  other elements.` | 一个或多个供应商特定元素可选地存在。该元素位于所有其他元素之后。 |

## 状态码(Status Code)

`Status Code`字段用于响应管理帧中，以指示所请求操作的成功或失败。状态码字段的长度为`2`个字节,**详情见IEEE 802.11-2016文档9.4.1.9**

| 编号        | 名称                                           | 含义(英文)                                                   | 含义(中文)                                                   |
| ----------- | ---------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `0`         | `SUCCESS`                                      | `Successful.`                                                | 成功                                                         |
| `1`         | `REFUSED,REFUSED_REASON_UNSPECIFIED`           | `Unspecified failure.`                                       | 未指定的失败                                                 |
| `2`         | `TDLS_REJECTED_ALTERNATIVE_PROVIDED`           | `TDLS wakeup schedule rejected but alternative schedule provided.` | `TDLS`唤醒调度被拒绝，但提供了替代调度                       |
| `3`         | `TDLS_REJECTED`                                | `TDLS wakeup schedule rejected.`                             | `TDLS`唤醒调度被拒绝                                         |
| `4`         |                                                | `Reserved.`                                                  | 保留                                                         |
| `5`         | `SECURITY_DISABLED`                            | `Security disabled.`                                         | 安全功能已禁用                                               |
| `6`         | `UNACCEPTABLE_LIFETIME`                        | `Unacceptable lifetime.`                                     | 生命周期不可接受                                             |
| `7`         | `NOT_IN_SAME_BSS`                              | `Not in same BSS.`                                           | 不在同一`BSS`中                                              |
| `8-9`       |                                                | `Reserved.`                                                  | 保留                                                         |
| `10`        | `REFUSED_CAPABILITIES_MISMATCH`                | `Cannot support all requested capabilities in the Capability Information field.` | 无法支持能力信息字段中请求的所有能力                         |
| `11`        | `DENIED_NO_ASSOCIATION_EXISTS`                 | `Reassociation denied due to inability to confirm that association exists.` | 由于无法确认关联已存在，重新关联被拒绝                       |
| `12`        | `DENIED_OTHER_REASON`                          | `Association denied due to reason outside the scope of this standard.` | 由于超出本标准范围的原因，关联被拒绝                         |
| `13`        | `UNSUPPORTED_AUTH_ALGORITHM`                   | `Responding STA does not support the specified authentication algorithm.` | 响应`STA`不支持指定的认证算法                                |
| `14`        | `TRANSACTION_SEQUENCE_ERROR`                   | `Received an Authentication frame with authentication transaction sequence number out of expected sequence.` | 接收到的认证帧中，认证事务序列号与预期序列不符               |
| `15`        | `CHALLENGE_FAILURE`                            | `Authentication rejected because of challenge failure.`      | 因挑战失败，认证被拒绝                                       |
| `16`        | `REJECTED_SEQUENCE_TIMEOUT`                    | `Authentication rejected due to timeout waiting for next frame in sequence.` | 因等待序列中下一帧超时，认证被拒绝                           |
| `17`        | `DENIED_NO_MORE_STAS`                          | `Association denied because AP is unable to handle additional associated STAs.` | 由于`AP`无法处理更多已关联的`STA`，关联被拒绝                |
| `18`        | `REFUSED_BASIC_RATES_MISMATCH`                 | `Association denied due to requesting STA not supporting all of the data rates in the BSSBasicRateSet parameter, the Basic HT-MCS Set field of the HT Operation parameter,or the Basic VHT-MCS and NSS Set field in the VHT Operation parameter.` | 由于请求`STA`不支持`BSSBasicRateSet`参数中的所有数据速率、`HT`操作参数中的`Basic HT-MCS Set`字段，或`VHT`操作参数中的`Basic VHT-MCS`和`NSS Set`字段，关联被拒绝 |
| `19`        | `DENIED_NO_SHORT_PREAMBLE_SUPPORT`             | `Association denied due to requesting STA not supporting the short preamble option.` | 由于请求`STA`不支持短前导码(`short preamble`)选项，关联被拒绝 |
| `20`        |                                                | `Reserved.`                                                  | 保留                                                         |
| `21`        |                                                | `Reserved`                                                   | 保留                                                         |
| 22          | REJECTED_SPECTRUM_MANAGE MENT_REQUIRED         | Association request rejected because Spectrum Management capability is required. | 由于需要频谱管理(`Spectrum Management`)能力，关联请求被拒绝  |
| `23`        | `REJECTED_BAD_POWER_CAPABILITY`                | `Association request rejected because the information in the Power Capability element is unacceptable.` | 由于功率能力(`Power Capability`)元素中的信息不可接受，关联请求被拒绝 |
| `24`        | `REJECTED_BAD_SUPPORTED_CHANNELS`              | `Association request rejected because the information in the Supported Channels element is unacceptable.` | 由于支持信道(`Supported Channels`)元素中的信息不可接受，关联请求被拒绝 |
| `25`        | `DENIED_NO_SHORT_SLOT_TIME_SUPPORT`            | `Association denied due to requesting STA not supporting the Short Slot Time option.` | 由于请求`STA`不支持短时隙(`Short Slot Time`)选项，关联被拒绝 |
| `26`        |                                                | `Reserved.`                                                  | 保留                                                         |
| `27`        | `DENIED_NO_HT_SUPPORT`                         | `Association denied because the requesting STA does not support HT features.` | 由于请求`STA`不支持`HT`特性，关联被拒绝                      |
| `28`        | `ROKH_UNREACHABLE`                             | `ROKH unreachable.`                                          | ROKH 不可达                                                  |
| `29`        | `DENIED_PCO_TIME_NOT_SUPPORTED`                | `Association denied because the requesting STA does not support the phased coexistence operation(PCO) transition time required by the AP` | 由于请求`STA`不支持`AP`所要求的分阶段共存操作(`PCO`)切换时间，关联被拒绝 |
| `30`        | `REFUSED_TEMPORARILY`                          | `Association request rejected temporarily,try again later.`  | 关联请求被临时拒绝，请稍后再试                               |
| `31`        | `ROBUST_MANAGEMENT_POLICY_VIOLATION`           | `Robust management frame policy violation.`                  | 鲁棒管理帧策略违规                                           |
| `32`        | `UNSPECIFIED_QOS_FAILURE`                      | `Unspecified,QoS-related failure.`                           | 未指定的`QoS`相关失败                                        |
| `33`        | `DENIED_INSUFFICIENT_BANDWIDTH`                | `Association denied because QoS AP or PCP has insufficient bandwidth to handle another QoS STA.` | 由于`QoS AP`或`PCP`没有足够的带宽处理另一个`QoS STA`，关联被拒绝 |
| `34`        | `DENIED_POOR_CHANNEL_CONDITIONS`               | `Association denied due to excessive frame loss rates and/or poor conditions on current operating channel.` | 由于当前工作信道上帧丢失率过高和/或信道条件不佳，关联被拒绝  |
| `35`        | `DENIED_QOS_NOT_SUPPORTED`                     | `Association(with QoS BSS)denied because the requesting STA does not support the QoS facility.` | 由于请求`STA`不支持`QoS`功能，与`QoS BSS`的关联被拒绝        |
| `36`        |                                                | `Reserved.`                                                  | 保留                                                         |
| `37`        | `REQUEST_DECLINED`                             | `The request has been declined.`                             | 请求已被拒绝                                                 |
| `38`        | `INVALID_PARAMETERS`                           | `The request has not been successful as one or more parameters have invalid values.` | 由于一个或多个参数具有无效值，请求未成功                     |
| `39`        | `REJECTED_WITH_SUGGESTED_CHANGES`              | `The allocation or TS has not been created because the request cannot be honored;however,a suggested TSPEC/DMG TSPEC is provided so that the initiating STA can attempt to set another allocation or TS with the suggested changes to the TSPEC/DMG TSPEC` | 由于无法满足请求，`TS`或分配未被创建；但提供了一个建议的`TSPEC/DMG TSPEC`，以便发起`STA`可根据建议修改后再次尝试建立分配或`TS` |
| `40`        | `STATUS_INVALID_ELEMENT`                       | `Invalid element,i.e.,an element defined in this standard for which the content does not meet the specifications in Clause 9.` | 无效元素，即本标准中定义的元素，但其内容不符合第`9`条款的规定 |
| `41`        | `STATUS_INVALID_GROUP_CIPHER`                  | `Invalid group cipher.`                                      | 无效组播加密套件                                             |
| `42`        | `STATUS_INVALID_PAIRWISE_CIPHER`               | `Invalid pairwise cipher.`                                   | 无效单播加密套件                                             |
| `43`        | `STATUS_INVALID_AKMP`                          | `Invalid AKMP.`                                              | 无效`AKMP`(认证与密钥管理协议)                               |
| `44`        | `UNSUPPORTED_RSNE_VERSION`                     | `Unsupported RSNE version.`                                  | 不支持的`RSNE`版本                                           |
| `45`        | `INVALID_RSNE_CAPABILITIES`                    | `Invalid RSNE capabilities.`                                 | 无效的`RSNE`能力                                             |
| `46`        | `STATUS_CIPHER_OUT_OF_POLICY`                  | `Cipher suite rejected because of security policy.`          | 由于安全策略，加密套件被拒绝                                 |
| `47`        | `REJECTED_FOR_DELAY_PERIOD`                    | `The TS or allocation has not been created;however,the HC or PCP might be capable of creating a TS or allocation,in response to a request,after the time indicated in the TS Delay element.` | `TS`或分配未被创建；但`HC`或`PCP`可能在`TS Delay`元素所指示的时间之后，有能力在收到请求时创建`TS`或分配 |
| `48`        | `DLS_NOT_ALLOWED`                              | `Direct linkis not allowed in the BSS by policy.`            | 根据策略，`BSS`中不允许直接链路                              |
| `49`        | `NOT_PRESENT`                                  | `The Destination STA is not present within this BSS`         | 目的`STA`当前不在该`BSS`内                                   |
| `50`        | `NOT_QOS_STA`                                  | `The Destination STA is not a QoS STA.`                      | 目的`STA`不是`QoS STA`                                       |
| `51`        | `DENIED_LISTEN_INTERVAL_TOO_LARGE`             | `Association denied because the listen interval is too large.` | 由于监听间隔(`listen interval`)过大，关联被拒绝              |
| `52`        | `STATUS_INVALID_FT_ACTION_FRAME_COUNT`         | `Invalid FT Action frame count.`                             | 无效的`FT Action`帧计数                                      |
| `53`        | `STATUS_INVALID_PMKID`                         | `Invalid pairwise master key identifier(PMKID).`             | 无效的成对主密钥标识符(`PMKID`)                              |
| `54`        | `STATUS_INVALID_MDE`                           | `Invalid MDE.`                                               | 无效的`MDE`(移动域元素)                                      |
| `55`        | `STATUS_INVALID_FTE`                           | `Invalid FTE.`                                               | 无效的`FTE`(快速过渡元素)                                    |
| `56`        | `REQUESTED_TCLAS_NOT_SUPPORTED`                | `Requested TCLAS processing is not supported by the AP or PCP.` | `AP`或`PCP`不支持所请求的`TCLAS`处理                         |
| `57`        | `INSUFFICIENT_TCLAS_PROCESSING_RESOURCES`      | `The APor PCP has insufficient TCLAS processing resources to satisfy the request.` | `AP`或`PCP`的`TCLAS`处理资源不足以满足请求                   |
| `58`        | `TRY_ANOTHER_BSS`                              | `The TS has not been created because the request cannot be honored;however,the HC or PCP suggests that the STA transition to a different BSS to set up the TS.` | 由于无法满足请求，`TS`未被创建；但`HC`或`PCP`建议`STA`切换到另一个`BSS`以建立`TS` |
| `59`        | `GAS_ADVERTISEMENT_PROTOCOL_NOT_SUPPORTED`     | `GAS Advertisement Protocol not supported.`                  | 不支持`GAS`广告协议                                          |
| `60`        | `NO_OUTSTANDING_GAS_REQUEST`                   | `No outstanding GAS request.`                                | 没有未完成的`GAS`请求                                        |
| `61`        | `GAS_RESPONSE_NOT_RECEIVED_FROM_SERVER`        | `GAS Response not received from the Advertisement Server.`   | 未从广告服务器收到`GAS`响应                                  |
| `62`        | `GAS_QUERY_TIMEOUT`                            | `STA timed out waiting for GAS Query Response.`              | `STA`等待`GAS`查询响应超时                                   |
| `63`        | `GAS_QUERY_RESPONSE_TOO_LARGE`                 | `GAS Response is larger than query response length limit.`   | `GAS`响应长度超过查询响应长度限制                            |
| `64`        | `REJECTED_HOME_WITH_SUGGESTED_CHANGES`         | `Request refused because home network does not support request.` | 由于归属网络不支持该请求，请求被拒绝                         |
| `65`        | `SERVER_UNREACHABLE`                           | `Advertisement Server in the network is not currently reachable.` | 网络中的广告服务器当前不可达                                 |
| `66`        |                                                | `Reserved.`                                                  | 保留                                                         |
| `67`        | `REJECTED_FOR_SSP_PERMISSIONS`                 | `Request refused due to permissions received via SSPN interface.` | 由于通过`SSPN`接口接收到的权限限制，请求被拒绝               |
| `68`        | `REFUSED_UNAUTHENTICATED_ACCESS_NOT_SUPPORTED` | `Request refused because the APor PCP does not support unauthenticated access.` | 由于`AP`或`PCP`不支持未经认证的访问，请求被拒绝              |
| `69-71`     |                                                | `Reserved.`                                                  | 保留                                                         |
| `72`        | `INVALID_RSNE`                                 | `Invalid contents of RSNE.`                                  | `RSNE`内容无效                                               |
| `73`        | `U_APSD_COEXISTANCE_NOT_SUPPORTED`             | `U-APSD coexistence is not supported.`                       | 不支持`U-APSD`共存                                           |
| `74`        | `U_APSD_COEX_MODE_NOT_SUPPORTED`               | `Requested U-APSD coexistence mode is not supported.`        | 所请求的`U-APSD`共存模式不被支持                             |
| `75`        | `BAD_INTERVAL_WITH_U_APSD_COEX`                | `Requested Interval/Duration value cannot be supported with U-APSD coexistence.` | 所请求的`Interval/Duration`值无法在`U-APSD`共存下支持        |
| `76`        | `ANTI_CLOGGING_TOKEN_REQUIRED`                 | `Authentication is rejected because an Anti-Clogging Token is required.` | 认证被拒绝，因为需要`Anti-Clogging Token`                    |
| `77`        | `UNSUPPORTED_FINITE_CYCLIC_GROUP`              | `Authentication is rejected because the offered finite cyclic group is not supported.` | 认证被拒绝，因为所提供的有限循环群不被支持                   |
| `78`        | `CANNOT_FIND_ALTERNATIVE_TBTT`                 | `The TBTT adjustment request has not been successful because the STA could not find an alternative TBTT.` | `TBTT`调整请求未成功，因为`STA`未能找到替代的`TBTT`          |
| `79`        | `TRANSMISSION_FAILURE`                         | `Transmission failure.`                                      | 传输失败                                                     |
| `80`        | `REQUESTED_TCLAS_NOT_SUPPORTED`                | `Requested TCLAS Not Supported.`                             | 所请求的 TCLAS 不支持                                        |
| `81`        | `TCLAS_RESOURCES_EXHAUSTED`                    | `TCLAS Resources Exhausted.`                                 | `TCLAS`资源耗尽                                              |
| `82`        | `REJECTED_WITH_SUGGESTED_BSS_TRANSITION`       | `Rejected with Suggested BSS transition.`                    | 建议进行`BSS`切换后被拒绝                                    |
| `83`        | `REJECT_WITH_SCHEDULE`                         | `Reject with recommended schedule.`                          | 建议采用推荐调度后被拒绝                                     |
| `84`        | `REJECT_NO_WAKEUP_SPECIFIED`                   | `Reject because no wakeup schedule specified.`               | 因未指定唤醒调度而被拒绝                                     |
| `85`        | `SUCCESS_POWER_SAVE_MODE`                      | `Success,the destination STA is in power save mode.`         | 成功，目的`STA`处于省电模式                                  |
| `86`        | `PENDING_ADMITTING_FST_SESSION`                | `FST pending,in process of admitting FST session.`           | `FST`待处理，正在接纳`FST`会话过程中                         |
| `87`        | `PERFORMING_FST_NOW`                           | `Performing FST now.`                                        | 立即执行`FST`                                                |
| `88`        | `PENDING_GAP_IN_BA_WINDOW`                     | `FST pending,gap(s)in block ack window.`                     | `FST`待处理，块确认窗口中存在间隙                            |
| `89`        | `REJECT_U-PID_SETTING`                         | `Reject because of U-PID setting`                            | 因`U-PID`设置被拒绝                                          |
| `90-91`     |                                                | `Reserved.`                                                  | 保留                                                         |
| `92`        | `REFUSED_EXTERNAL_REASON`                      | `(Re)Association refused for some external reason.`          | 由于某些外部原因，(重)关联被拒绝                             |
| `93`        | `REFUSED_AP_OUT_OF_MEMORY`                     | `(Re)Association refused because of memory limits at the AP.` | 由于`AP`内存限制，(重)关联被拒绝                             |
| `94`        | `REJECTED_EMERGENCY_SERVICES_NOT_SUPPORTED`    | `(Re)Association refused because emergency services are not supported at the AP.` | 由于`AP`不支持紧急服务，(重)关联被拒绝                       |
| `95`        | `QUERY_RESPONSE_OUTSTANDING`                   | `GAS query response not yet received.`                       | 尚未收到`GAS`查询响应                                        |
| `96`        | `REJECT_DSE_BAND`                              | `Reject since the request is for transition to a frequency band subject to DSE procedures and FST Initiator is a dependent STA.` | 由于请求涉及需遵循`DSE`程序的频段，且`FST`发起方为依赖型`STA`，因此被拒绝 |
| `97`        | `TCLAS_PROCESSING_TERMINATED`                  | `Requested TCLAS processing has been terminated by the AP.`  | 所请求的`TCLAS`处理已被`AP`终止                              |
| `98`        | `TS_SCHEDULE_CONFLICT`                         | `The TS schedule conflicts with an existing schedule;an alternative schedule is provided.` | `TS`调度与现有调度冲突；已提供替代调度                       |
| `99`        | `DENIED_WITH_SUGGESTED_BAND_AND_CHANNEL`       | `The association has been denied;however,one or more Multi-band elements are included that can be used by the receiving STA to join the BSS.` | 关联被拒绝；但包含一个或多个多频段(`Multi-band`)元素，接收`STA`可利用这些元素加入`BSS` |
| `100`       | `MCCAOP_RESERVATION_CONFLICT`                  | `The request failed due to a reservation conflict.`          | 由于预留冲突，请求失败                                       |
| `101`       | `MAF_LIMIT_EXCEEDED`                           | `The request failed due to exceeded MAF limit.`              | 由于超出`MAF`限制，请求失败                                  |
| `102`       | `MCCA_TRACK_LIMIT_EXCEEDED`                    | `The request failed due to exceeded MCCA track limit.`       | 由于超出`MCCA`跟踪限制，请求失败                             |
| `103`       | `DENIED_DUE_TO_SPECTRUM_MANAGEMENT`            | `Association denied because the information in the Spectrum Management field is unacceptable.` | 由于频谱管理字段中的信息不可接受，关联被拒绝                 |
| `104`       | `DENIED_VHT_NOT_SUPPORTED`                     | `Association denied because the requesting STA does not support VHT features.` | 由于请求`STA`不支持`VHT`特性，关联被拒绝                     |
| `105`       | `ENABLEMENT DENIED`                            | `Enablement denied.`                                         | 启用被拒绝                                                   |
| `106`       | `RESTRICTION FROM AUTHORIZED GDB`              | `Enablement denied due to restriction from an authorized GDB.` | 由于来自授权`GDB`的限制，启用被拒绝                          |
| `107`       | `AUTHORIZATION DEENABLED`                      | `Authorization deenabled.`                                   | 授权已被禁用                                                 |
| `108-65535` |                                                | `Reserved.`                                                  | 保留                                                         |

## 响应码(Reason Code)

`Reason Code`字段用于指示生成类型为解除关联(`Disassociation`)、解除认证(`Deauthentication`)、`DELTS`、`DELBA`、`DLS`拆除(`DLS Teardown`)或`Mesh`对等关闭(`Mesh Peering Close`)的非请求通知管理帧的原因。它包含在`Mesh`信道切换参数(`Mesh Channel Switch Parameters`)元素中，用以指示信道切换的原因；也包含在`PERR`(`Path Error`)元素中，用以指示路径错误的原因。原因码字段的长度为`2`个字节；**详情见IEEE 802.11-2016文档9.4.1.7**

| Reason code | Name                                            | Meaning                                                      | 含义                                                         |
| ----------- | ----------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `0`         |                                                 | `Reserved`                                                   | 保留                                                         |
| `1`         | `UNSPECIFIED_REASON`                            | `Unspecified reason`                                         | 未指定原因                                                   |
| `2`         | `INVALID_AUTHENTICATION`                        | `Previous authentication no longer valid`                    | 先前的认证不再有效                                           |
| `3`         | `LEAVING_NETWORK_DEAUTH`                        | `Deauthenticated because sending STA is leaving(or has left  IBSS or ESS` | 因发送`STA`正在离开(或已离开)`IBSS`或`ESS`而解除认证         |
| `4`         | `REASON_INACTIVITY`                             | `Disassociated due to inactivity`                            | 因不活动而解除关联                                           |
| `5`         | `NO_MORE_STAS`                                  | `Disassociated because AP is unable to handle all currently associated STAs` | 因`AP`无法处理当前所有已关联的`STA`而解除关联                |
| `6`         | `INVALID_CLASS2_FRAME`                          | `Class 2 frame received from nonauthenticated STA`           | 从未认证的`STA`收到`Class 2`帧                               |
| `7`         | `INVALID_CLASS3_FRAME`                          | `Class 3 frame received from nonassociated STA`              | 从未关联的`STA`收到`Class 3`帧                               |
| `8`         | `LEAVING_NETWORK_DISASSOC`                      | `Disassociated because sending STA is leaving(or has left) BSS` | 因发送`STA`正在离开(或已离开)`BSS`而解除关联                 |
| `9`         | `NOT_AUTHENTICATED`                             | `STA requesting (re)association is not authenticated with responding STA` | 请求(重)关联的`STA`未与响应`STA`完成认证                     |
| `10`        | `UNACCEPTABLE_POWER_CA PABILITY`                | `Disassociated because the information in the Power Capability element is unacceptable` | 因功率能力(`Power Capability`)元素中的信息不可接受而解除关联 |
| `11`        | `UNACCEPTABLE_SUPPORTED _CHANNELS`              | `Disassociated because the information in the Supported Channels element is unacceptable` | 因支持信道(`Supported Channels`)元素中的信息不可接受而解除关联 |
| `12`        | `BSS_TRANSITION_DISASSOC`                       | `Disassociated due to BSS transition management`             | 因`BSS`切换管理而解除关联                                    |
| `13`        | `REASON_INVALID_ELEMENT`                        | `Invalid element,i.e.,an element defined in this standard for which the content does not meet the specifications in Clause 9` | 无效元素，即本标准中定义的某个元素，其内容不符合第`9`条款中的规范 |
| `14`        | `MIC_FAILURE`                                   | `Message integrity code(MIC)failure`                         | 消息完整性校验码(`MIC`)失败                                  |
| `15`        | `4WAY_HANDSHAKE_TIMEOUT`                        | `4-way handshake timeout`                                    | 四次握手超时                                                 |
| `16`        | `GK_HANDSHAKE_TIMEOUT`                          | `Group key handshake timeout`                                | 组密钥握手超时                                               |
| `17`        | `HANDSHAKE_ELEMENT_MISMATCH`                    | `Element in 4-way handshake different from(Re)Association Request/Probe Response/Beacon frame` | 四次握手中的元素与(重)关联请求/探测响应/信标帧中的不一致     |
| `18`        | `REASON_INVALID_GROUP_CIPHER`                   | `Invalid group cipher`                                       | 无效的组播加密套件                                           |
| `19`        | `REASON_INVALID_PAIRWISE _CIPHER`               | `Invalid pairwise cipher`                                    | 无效的单播加密套件                                           |
| `20`        | `REASON_INVALID_AKMP`                           | `Invalid AKMP`                                               | 无效的认证与密钥管理协议(`AKMP`)                             |
| `21`        | `UNSUPPORTED_RSNE_VERSION`                      | `Unsupported RSNE version`                                   | 不支持的`RSNE`版本                                           |
| `22`        | `INVALID_RSNE_CAPABILITIES`                     | `Invalid RSNE capabilities`                                  | 无效的`RSNE`能力字段                                         |
| `23`        | `802_1_X_AUTH_FAILED`                           | `IEEE 802.1Xauthentication failed`                           | `IEEE 802.1X`认证失败                                        |
| `24`        | `REASON_CIPHER_OUT_OFPOLICY`                    | `Cipher suite rejected because of the security policy`       | 因安全策略拒绝该加密套件                                     |
| `25`        | `TDLS_PEER_UNREACHABLE`                         | `TDLS direct-link teardown due to TDLS peer STA unreachable via the TDLS direct link` | 因通过`TDLS`直连链路无法到达对端`TDLS`站点，导致`TDLS`直连链路拆除 |
| `26`        | `TDLS_UNSPECIFIED_REASON`                       | `TDLS direct-link teardown for unspecified reason`           | 因未指定原因导致`TDLS`直连链路拆除                           |
| `27`        | `SSP_REQUESTED_DISASSOC`                        | `Disassociated because session terminated by SSP request`    | 因`SSP`请求终止会话而解除关联                                |
| `28`        | `NO_SSP_ROAMING_AGREEMENT`                      | `Disassociated because of lack of SSP roaming agreement`     | 因缺少`SSP`漫游协议而解除关联                                |
| `29`        | `BAD_CIPHER_OR_AKM`                             | `Requested service rejected because of SSP cipher suite or AKM requirement` | 因`SSP`对加密套件或`AKM`的要求未满足，所请求的服务被拒绝     |
| `30`        | `NOT_AUTHORIZED_THIS_LOCATION`                  | `Requested service not authorized in this location`          | 在此位置未授权使用所请求的服务                               |
| `31`        | `SERVICE_CHANGE_ PRECLUDES_TS`                  | `TS deleted because QoS APlacks sufficient bandwidth for this QoS STA due to a change in BSS service characteristics or operational mode(e.g.,an HT BSS change from 40 MHz channel to 20 MHz channel)` | 由于`BSS`服务特性或运行模式变更(例如，`HT BSS`从`40 MHz`信道切换至`20 MHz`信道)，`QoS AP`无法为此`QoS STA`提供足够带宽，从而删除`TS` |
| `32`        | `UNSPECIFIED_QOS_REASON`                        | `Disassociated for unspecified,QoS-related reason`           | 因未指定的`QoS`相关原因而解除关联                            |
| `33`        | `NOT_ENOUGH_BANDWIDTH`                          | `Disassociated because QoS AP lacks sufficient bandwidth for this QoS STA` | 因`QoS AP`无法为此`QoS STA`提供足够带宽而解除关联            |
| `34`        | `MISSING_ACKS`                                  | `Disassociated because excessive number of frames need to be acknowledged,but are not acknowledged due to AP transmissions and/or poor channel conditions` | 因需确认的帧数量过多，但由于`AP`发送和/或信道条件差而未能确认，从而解除关联 |
| `35`        | `EXCEEDED_TXOP`                                 | `Disassociated because STA is transmitting outside the limits of its TXOPs` | 因`STA`在其`TXOP`限制之外进行发送而解除关联                  |
| `36`        | `STA_LEAVING`                                   | `Requesting STA is leaving the BSS(or resetting)`            | 请求方`STA`正在离开`BSS`(或正在重置)                         |
| `37`        | `END_TS END_BA END_DLS`                         | `Requesting STA is no longer using the stream or session`    | 请求方`STA`不再使用该流或会话                                |
| `38`        | `UNKNOWN_TS UNKNOWN_BA`                         | `Requesting STA received frames using a mechanism for which a setup has not been completed` | 请求方`STA`接收到使用尚未完成建立机制传输的帧                |
| `39`        | `TIMEOUT`                                       | `Requested from peer STA due to timeout`                     | 因超时由对端`STA`请求                                        |
| `45`        | `PEERKEY_MISMATCH`                              | `Peer STA does not support the requested cipher suite`       | 对端`STA`不支持所请求的加密套件                              |
| `46`        | `PEER_INITIATED`                                | `In a DLS Teardown frame:The teardown was initiated by the DLS peer In a Disassociation frame:Disassociated because authorized access limit reached` | 在`DLS`拆除帧中：拆除由`DLS`对端发起；在解除关联帧中：因达到授权访问上限而解除关联 |
| `47`        | `AP_INITIATED`                                  | `In a DLS Teardown frame:The teardown was initiated by the AP In a Disassociation frame:Disassociated due to external service requirements` | 在`DLS`拆除帧中：拆除由`AP`发起；在解除关联帧中：因外部服务需求而解除关联 |
| `48`        | `REASON_INVALID_FT_ACTIO N_FRAME_COUNT`         | `Invalid FTAction frame count`                               | 无效的`FT Action`帧计数                                      |
| `49`        | `REASON_INVALID_PMKID`                          | `Invalid pairwise master key identifier(PMKID)`              | 无效的成对主密钥标识符(`PMKID`)                              |
| `50`        | `REASON_INVALID_MDE`                            | `Invalid MDE`                                                | 无效的`MDE`(移动域元素)                                      |
| `51`        | `REASON_INVALID_FTE`                            | `Invalid FTE`                                                | 无效的`FTE`(快速过渡元素)                                    |
| `52`        | `MESH-PEERING-CANCELED`                         | `Mesh peering canceled for unknown reasons`                  | 因未知原因取消`Mesh`对等连接                                 |
| `53`        | `MESH-MAX-PEERS`                                | `The mesh STA has reached the supported maximum number of peer mesh STAs` | `Mesh`站点已达到所支持的最大对等`Mesh`站点数量               |
| `54`        | `MESH-CONFIGURATION- POLICY-VIOLATION`          | `The received information violates the Mesh Configuration policy configured in the mesh STA profile` | 接收到的信息违反了`Mesh`站点配置文件中设定的`Mesh`配置策略   |
| `55`        | `MESH-CLOSE-RCVD`                               | `The mesh STA has received a Mesh Peering Close frame requesting to close the mesh peering` | `Mesh`站点收到请求关闭`Mesh`对等连接的`Mesh Peering Close`帧 |
| `56`        | `MESH-MAX-RETRIES`                              | `The mesh STA has resent dot11MeshMaxRetries Mesh Peering Open frames,without receiving a Mesh Peering Confirm frame.` | `Mesh`站点已重传`dot11MeshMaxRetries`次`Mesh Peering Open`帧，但未收到`Mesh Peering Confirm`帧 |
| `57`        | `MESH-CONFIRM-TIMEOUT`                          | `The confirm Timer for the mesh peering instance times out.` | 该`Mesh`对等实例的确认定时器超时                             |
| `58`        | `MESH-INVALID-GTK`                              | `The mesh STA fails to unwrap the GTK or the values in the wrapped contents do not match` | `Mesh`站点无法解封装`GTK`，或封装内容中的值不匹配            |
| `59`        | `MESH-INCONSISTENT- PARAMETERS`                 | `The mesh STA receives inconsistent information about the mesh parameters between mesh peering Management frames` | `Mesh`站点在不同`Mesh`对等管理帧中接收到的`Mesh`参数信息不一致 |
| `60`        | `MESH-INVALID-SECURITY- CAPABILITY`             | `The mesh STA fails the authenticated mesh peering exchange because due to failure in selecting either the pairwise ciphersuite or group ciphersuite` | `Mesh`站点在认证的`Mesh`对等交换过程中失败，原因是未能成功协商单播或组播加密套件 |
| `61`        | `MESH-PATH-ERROR-NO- PROXY-INFORMATION`         | `The mesh STA does not have proxy information for this external destination.` | `Mesh`站点没有此外部目的地址的代理信息                       |
| `62`        | `MESH-PATH-ERROR-NO- FORWARDING-INFORMATION`    | `The mesh STA does not have forwarding information for this destination.` | `Mesh`站点没有此目的地址的转发信息                           |
| `63`        | `MESH-PATH-ERROR- DESTINATION- UNREACHABLE`     | `The mesh STA determines that the link to the next hop of an active path in its forwarding information is no longer usable.` | `Mesh`站点判定其转发表中某条活跃路径的下一跳链路已不可用     |
| `64`        | `MAC-ADDRESS-ALREADY- EXISTS-IN-MBSS`           | `The Deauthentication frame was sent because the MAC address of the STA already exists in the mesh BSS See 11.3.6.` | 发送解除认证帧，因为该 STA 的 MAC 地址已在`Mesh BSS`中存在   |
| `65`        | `MESH-CHANNEL-SWITCH- REGULATORY- REQUIREMENTS` | `The mesh STA performs channel switch to meet regulatory requirements.` | `Mesh`站点为满足监管要求而执行信道切换                       |
| `66`        | `MESH-CHANNEL-SWITCH- UNSPECIFIED`              | `The mesh STA performs channel switching with unspecified reason.` | `Mesh`站点因未指定原因执行信道切换                           |
| `67-65535`  |                                                 | `Reserved`                                                   | 保留                                                         |

## Action码

`Action`字段提供了一种用于指定扩展管理操作的机制

| Code      | Meaning                                  | Chinese                          |
| --------- | ---------------------------------------- | -------------------------------- |
| `0`       | `Spectrum Management`                    | 频谱管理                         |
| `1`       | `QoS`                                    | 服务质量                         |
| `2`       | `DLS`                                    | 直连链路建立                     |
| `3`       | `Block Ack`                              | 块确认                           |
| `4`       | `Public`                                 | 公共动作                         |
| `5`       | `Radio Measurement`                      | 无线测量                         |
| `6`       | `Fast BSS Transition`                    | 快速`BSS`切换                    |
| `7`       | `HT`                                     | 高吞吐量                         |
| `8`       | `SA Query`                               | 安全关联查询                     |
| `9`       | `Protected Dualof Public Action`         | 保护性公共行动与公众行动的双重性 |
| `10`      | `WNM`                                    | 无线网络管理                     |
| `11`      | `Unprotected WNM`                        | 非保护`WNM`                      |
| `12`      | `TDLS`                                   | `TDLS`                           |
| `13`      | `Mesh`                                   | `Mesh`                           |
| `14`      | `Multihop`                               | 多跳                             |
| `15`      | `Self-protected`                         | 自保护                           |
| `16`      | `DMG`                                    | 定向多千兆                       |
| `17`      | `Reserved(used by the Wi-Fi Alliance®a)` | 保留(由`Wi-Fi 联盟®`使用)        |
| `18`      | `Fast Session Transfer`                  | 快速会话迁移                     |
| `19`      | `Robust AV Streaming`                    | 稳健的车载视频流传输             |
| `20`      | `Unprotected DMG`                        | 非保护`DMG`                      |
| `21`      | `VHT`                                    | 甚高吞吐量                       |
| `21–125`  | `Reserved`                               | 保留                             |
| `126`     | `Vendor-specific Protected`              | 供应商专用保护                   |
| `127`     | `Vendor-specific`                        | 供应商特定                       |
| `128–255` | `Error`                                  | 错误                             |

## 参考文档

> 《无线局域网权威指南 第五版》
>
> [80211-2016规范](https://github.com/Garfield-1/Books/blob/master/11_802.11协议规范/80211-2016.pdf)